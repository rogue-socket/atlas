# Backlog

Durable "someday/maybe" items — distinct from session-level Unresolved (which is "next session"). Each entry: one-line item specific enough to act on cold, optional priority, **Why:** if non-obvious.

<!-- from: docs/STATUS.md (What's Next, as of 2026-04-29) -->
<!-- from: docs/TODO.md (unchecked items, as of 2026-05-04) -->

## Bugs

<!-- Fixed 2026-05-11: RecentFilesManager.autoRemoveStaleFiles never fired (init's sync call read inaccessibleFiles before async file-check populated it). Restructured init to dispatch as barrier on fileCheckQueue → main. 3 new RecentFilesManagerTests. -->

<!-- Fixed 2026-05-07 (wip/feature-cherry-pick): cross-doc in-memory graph leak; deleteProject cleanup; analyze-mode threading; GraphStore content-invalidation (StoredGraph wrapper); hierarchyLevel decode default. -->
<!-- Fixed 2026-05-08 (3a0f813 + uncommitted): JSONRepair closes truncated edges arrays; .document zoom prefers ConceptNode.isDocumentSummary via appendDocumentSummary. -->
<!-- Fixed 2026-05-15: "Modifying state during view update" warning across tab switches — wrapped PDFViewerView.swift onPageChanged @State write in DispatchQueue.main.async. -->
<!-- Fixed 2026-05-15: stale Coordinator closures in PDFViewRepresentable across tab switches — three closure props flipped `let` → `var`, refreshed in updateNSView alongside annotationMode etc. -->
<!-- Fixed 2026-05-15: double-decode on graph hydration — GraphStore.loadPayload(for:) returns raw bytes; loadGraphIfNeeded decodes once into the @Environment-injected graph. -->

<!-- Fixed 2026-05-17 (commit 471c5ca): GraphStore.sweepOrphans was deleting embedding caches + ETR audit sidecars alongside legacy project_* files — extracted isSweepablePerDocGraphFile classifier; 5 GraphStoreSweepTests. -->

## Active / Next

- [next 2026-05-20] **Claude-subscription extraction backend — shipped, follow-ups open.** `feature/claude-subscription-sidecar` @ `1bee21c` pushed to origin (7 files, +331). `claude-sidecar/server.mjs` (Node, builtins-only) wraps `claude -p` headless with `ANTHROPIC_API_KEY` stripped → subscription auth; `--tools ""` + minimal `--system-prompt` trim agent overhead. New `ClaudeSidecarBackend` (`LLMBackend`) + `AIBackendType.claudeSubscription` wired through `AIServiceManager`/`AISettingsView`/`Constants`. Verified: 4-PDF NexaPay "Analyze All" → 24 batches on Opus, 0 failures, ~22 min. **Follow-ups:** (a) open PR (`github.com/rogue-socket/atlas` → the branch); (b) `atlas/CLAUDE.md` still says "4 backends (Claude, OpenAI, Gemini, Ollama)" — now 5; (c) sidecar has no auto-start — run `node atlas/claude-sidecar/server.mjs` before extracting; (d) subscription rate limits apply, ~75–90s/batch on Opus.
- [active 2026-05-17] **ETR cross-doc merging — branch pushed, audit sidecar captured, tuning loop unblocked.** `feature/etr-cross-doc` @ `fefcf8c` synced to origin (merge of `main` brought in the 2026-05-17 sweep-classifier fix). Live `--etr-only` run on vitacare at 2026-05-17 00:15 (commit-state `5d72558` + sweep-fix): 246n/753e baseline, 218 eligible, 17810 pairs, 50 adjudication band, 6 LLM-approved / 44 rejected, 3 groups applied → post-graph 240n/747e in 51s wall-clock. Audit sidecar at `Atlas/graphs/etr_audit_ABE6D4F9-…_2026-05-16T18-46-34Z.json` (50 entries, full per-pair JSON). 100% precision on the 6 approved against PRD v2 rubric; 2 clear false-negatives in top-12 rejected → see prompt-tuning entry below for the concrete pairs. Embedding cache (8.57 MB, 218 entries) now persistent across launches thanks to the sweep fix; next iteration is the ~13s warm-cache promise. Sweep details: `audits/2026-05-16_etr-live-verification.md` (earlier 0.85/0.80/0.75 floor sweep); 81 tests green across the ETR bundle pre-merge. **Major correction (carried):** `GraphMergeEngine` is dormant code — `MergeProposalView` never instantiated. Integration decision #2 ("disable GraphMergeEngine during A/B runs") is moot per PRD §"Integration decisions" stale-marker; cross-doc baseline merges come from `KnowledgeGraph.node(matching:)`. Quality Rubric v2 in PRD §"Quality Rubric v2 — vitacare 2026-05-16".
<!-- Fixed 2026-05-17 (06f0b4b): side-track (a) — orphan-sweep was deleting per-doc graphs when RecentFilesManager.loadRecentFiles destroyed bookmarks on first failed resolve. Bookmarks now preserved via pathFromBookmark fallback; 3-launch counter owns cleanup. 2 new RecentFilesManagerTests. -->
<!-- Fixed 2026-05-17 (commits 26078a4..18f7bf7): UX "Re-select…" affordance — RecentFilesManager.replaceBookmark + sibling "Locate…" button in recents row + .fileNotReadable alert offering Locate/Remove. 3 new RecentFilesManagerTests. Not yet live-smoke-tested. -->
<!-- Fixed 2026-05-17 (9afcc07): side-track (b) — ExtractionPipeline.processBatch was skipping per-doc scheduleSave when projectID was set. Always calls scheduleSave now. -->
<!-- Fixed 2026-05-17 (f7a4fcb + 65e342b + 6b8049f): cleanup — removed write-only project-graph API (saveProjectGraph/loadProjectGraph/deleteProjectGraph/projectGraphFileURL) and the dead projectID param from pipeline entry points. Legacy project_*.json now sweepable. -->

- [active 2026-05-17] **Recall-improvement experiment for ETR — prompt tuning DONE on `feature/etr-cross-doc` @ `1934849` (pushed origin), merge-to-`main` pending.** Outcome (3-of-3 stable across runs with Gemini T=0+topK=1 from prior commit `1b0c3d1`): rubric recall **3-4/7 → 7/7** (rows 10, 13 — the two named hard targets — both flipped 0/3 → 3/3; row 3 cross-level Lab Result Communication ↔ Lab result release newly caught too). Rubric precision held at **8/8**. v1 of the revision regressed on a `<leaf service> ↔ "VitaCare Services"` catalog-FP cluster (1-3 per run); v2 added an explicit "leaf-of-catalog → use hierarchy edge" anti-pattern with 4 examples and the regression went to 0/15 across 3 v2 runs. Post-tune project graph 235n/740e (vs old prompt 240n/747e, vs pre-ETR 246n/753e). 11 audit sidecars on disk as regression dataset (1 yesterday + 1 baseline-tonight + 3 det-old + 3 v1-tuned + 3 v2-tuned). PRD v2 rubric updated to mark rows 3/10/13 as caught + appended a 2026-05-17 score block. Methodology finding: Gemini at T=0+K=1 is NOT fully deterministic (4-5/5/5 same-prompt counts on old prompt across 3 runs) — diagnostic reads use stable 3-of-3 intersection per pair, not single-run approval counts. Full method + off-rubric extras vetted in `backlog.md` (on `feature/etr-cross-doc`) Done block.
- [next 2026-05-19] **Issue #33 final site — `toggleFullscreen` state read (`PDFViewerView.swift:334`).** `DispatchQueue.main.asyncAfter(deadline: .now() + 0.2)` waits for AppKit to finish the fullscreen transition before reading `window.styleMask.contains(.fullScreen)`. Different bug class from the layout-race sites fixed 2026-05-19 (`:54` + `:413` DONE) — this one is post-AppKit-transition state. Replace with `.onReceive(NotificationCenter.default.publisher(for: NSWindow.didEnterFullScreenNotification))` + `.didExitFullScreenNotification` at the view-body level; route both to a shared `syncFullscreenState(_:)` helper that owns `isFullscreen` + `UserDefaults` + `hideToolbarInFullscreen` resets. Bonus: catches user-driven transitions (menu, hotkey) that `toggleFullscreen` doesn't, eliminating a class of state-drift bugs. ~15 LOC. Last remaining `asyncAfter` sub-second site in PDFViewerView per the issue.
- [next 2026-05-19] **Cross-branch backlog sync after /clear.** Today's session added entries to main-branch backlog (Issue #33 progress, Y-coord audit DONE, [active 2026-05-12] uncommitted progress on annotation resize) that aren't reflected in `feature/etr-cross-doc` or `feature/sce-cross-doc` backlogs. Two stashes carry the parallel edits: `stash` on ETR with the cache-staleness DONE + SCE-run inconclusive blocks, `stash` on SCE with the SCE-side SCE-run-inconclusive block. Pick one of: (a) merge main into both feature branches (carries the doc changes wholesale, surfaces any code conflicts); (b) cherry-pick the backlog/audit additions manually onto each branch; (c) accept divergence and rely on main as the source-of-truth for cross-branch records. Default leans (a) since main is also where the annotation-resize working-tree changes need to land via PR.
- [next 2026-05-19] **CLAUDE.md gotcha refresh — Y-coord warning replaced (workspace-level).** Updated 2026-05-19: the "annotation coordinate conversion is manual ... may have Y-coordinate inversion issues" warning was inaccurate. New warning calls out the two real PDFKit-quirk sites (line/arrow `linePoints` + ink `annotation.add(path)` populated with page-absolute coords). NOT in a git repo — this is `/Users/yashagrawal/Documents/pdf_projects/CLAUDE.md`. Full audit in `audits/2026-05-19_annotation-coord-audit.md`. Closed [active 2026-05-12]-adjacent concern. **Verification action if curious:** 5-min smoke-test that drags an ink stroke + then drags the resulting annotation (select-mode body-drag) — visible ink should move WITH the annotation. If it stays anchored in place while the annotation rect moves, the page-absolute interpretation has flipped on this macOS version.
- [next 2026-05-16] **Recall-improvement experiments for ETR — remaining sub-items.** Each is its own API-cost-heavy session: (b) embedding model swap — try `gemini-embedding-001` or OpenAI `text-embedding-3-large` for a different vector space (only after prompt tuning to isolate effects); (c) per-level threshold split — concept↔concept may want a different floor than entity↔entity; (d) in-doc pair support — one-line filter change in `pairsToCompare`, opens up the 5 in-doc rubric v1 rows + many more candidates.
<!-- Done 2026-05-20 (8636ba4 on feature/claude-subscription-sidecar): Annotation move/resize — `.select` mode landed end to end (body-drag translate + corner/edge resize via AnnotationGeometry, SelectionChromeOverlay 1pt outline + 8 handles, click-select, hover cursors, keyboard delete gated on the pdfView firstResponder chain). Built across PR #49 (translate) + 2026-05-19 (resize/delete) + 2026-05-20 (chrome/click/hover). 2026-05-20 smoke-test found + fixed 2 bugs: resize flakiness (`.began` hit-test now uses the true mouse-down point captured via HighlightingPDFView.mouseDown, not the post-threshold gesture location) and dead hover cursor (acceptsMouseMovedEvents set in viewDidMoveToWindow + PDFView cursor-rect suppression while over the pdfView in select mode). Verified CGEvent + user real-mouse — chrome, translate, resize, hover cursors, delete, undo, firstResponder delete-gate all pass; 10/10 AnnotationGeometryTests. Detail: ~/.claude/sessions/atlas/2026-05-20.md. -->
- [next 2026-05-14] Port `rogue-socket/issue-11` (guided tour, #11) — last unmerged branch. ~700 unique lines: new `Atlas/Tour/` directory (`GuidedTour`, `TourGenerator`, `TourPlayer`, `TourPlaybackView`, `TourPlayerTests`) + `MapInteraction.focusOnNode` + `KnowledgeMapView` wiring. Apply same audit-diff-adapt-commit pattern used for grounded-chatbot today. Will need a real port (not an already-merged finding).
- [next 2026-05-14] PR-A progress: 3 of 6 closed (#47 ✅, #38 ✅, #35 closed obsolete). Remaining are gated/deferred: #29 (FDL — adjacent to deferred Tier 1 #3), #28 (gated on deferred #27), #27 (deferred no-profile). All three need profiling evidence before reopening.
- [active 2026-05-17] **Cross-doc merging — both branches LIVE on origin; ETR audit dataset captured.** `feature/sce-cross-doc` @ `e763c8a` (8 ETR-prep commits + 2026-05-17 merge of `main`) and `feature/etr-cross-doc` @ `fefcf8c` (13 ETR commits + 2026-05-17 merge of `main`) both pushed. ETR is no longer "pending" per the earlier ordering note — both branches are live; the 2026-05-17 `--etr-only` run produced the first per-pair audit sidecar (see ETR entry above for run results + the prompt-tuning entry for next-action). **SCE end-to-end run attempted 2026-05-19 16:04–16:11 on SCE branch @ `381df79` — INCONCLUSIVE.** Rate-limited (6× Gemini HTTP 429s on the final doc, `patient_experience_and_operations.pdf`) cut OPS to 4 nodes vs ~30–40 on the other three. Run produced 208 vitacare nodes total but only **1 cross-doc merged node** ("Care Between Visits" CLI+ORG entity, not in the 20+20 rubric) and **0 SCE-typed edges** (instanceOf / attributeOf / processFor) — none fired despite wiring being verified in the 11:00 cross-branch review. Rubric scoring uninterpretable as-run because PRD labels (grounded in 2026-05-16 extraction) drift across re-extractions — 17/20 SHOULD-NOT-MERGE pairs returned MISSING on both sides. Full diagnostics + open questions in `audits/2026-05-19_sce-end-to-end-vitacare-rate-limited.md`. Cost spent: ~$0.30–$0.60 Flash, not recovered. **Next attempt needs:** (a) Gemini quota window reset or switch to Pro key; (b) `log.info` instrumentation at `resolveMatchAction` entry/exit to distinguish "SCE fired but LLM chose same_entity" from "SCE wiring bypassed in headless path"; (c) embedding-similarity rubric scoring (vs exact-label match) — ~30 LOC reusing `EmbeddingResolver.contentHash`. Design doc: `prds/2026-05-15_4-level-knowledge-graph.md` §"Cross-Doc Merging — Two Approaches" (lines 139-222) + §"Comparison Conditions" (224-268) + §"Locked-in prep items — 2026-05-16". Decision #11 mandates building BOTH on separate branches, A/B testing, winner merges back to `main`. **SCE step 1 plan + file-touch list + tests** captured in `~/.claude/sessions/atlas/2026-05-16.md` under `/wrap before context clear`.
  - **SCE — Sequential Cumulative Extraction** (`feature/sce-cross-doc`). Doc N's every batch carries cumulative state (every node + edge from docs 1..N-1) in the prompt header. LLM prompted: "reuse only when same real-world thing; type advisory." Atomic per-doc commits. Optional final canonicalization LLM pass. **Infra: low** — no new backend, no new settings. **Risks:** quadratic prompt tokens (alarm at >50% context window), anchoring (doc 1 dominates), hallucinated reuse, granularity drift. **Observability from day 1:** `[SCE] doc=X batch=Y prompt_tokens=N` per batch, `new/reused/new_relation_types` per doc, reuse-mapping audit log.
  - **ETR — Extract-Then-Resolve** (`feature/etr-cross-doc`). Four stages: (1) independent per-doc extraction (parallel-eligible), (2) pool + embed `label + ": " + type + summary` + brute-force pairwise candidates, (3) tiered resolution — auto-merge (exact match or sim > 0.95), batched LLM adjudication for sim 0.85-0.95 (~15-20 pairs/call), auto-reject for sim < 0.85, (4) merge + canonical labels + level promotion + edge dedup. **Infra: higher** — new Embedding Model selector in Settings (OpenAI text-embedding-3-small/-large, Gemini text-embedding-004, Ollama nomic-embed-text; Claude has none). ETR disabled in UI when no embedding configured; SCE still works. **Thresholds are placeholders** — must be tuned empirically; re-run only stages 2-4 to retune (cheap).
  - **A/B rubric (frozen before either runs):** held constant = corpus + extraction LLM + machine + α-foundation. Captured = wall-clock, token cost, total nodes, total edges, cross-doc shared entities (`sourceAnchors.count > 1`), cross-doc edges. Quality = 20 should-merge + 20 should-not-merge pairs hand-graded → precision + recall. Re-run: SCE shuffles doc order; ETR varies threshold.
  - **Three prep items the PRD requires before any branch code lands — LOCKED IN 2026-05-16** (full detail in PRD §"Locked-in prep items — 2026-05-16"):
    1. ✅ **Test corpus:** `vitacare` 4-PDF set in `sample_pdfs/files/vitacare_*.pdf` (organization_and_people, clinical_services_and_pricing, patient_experience_and_operations, compliance_quality_and_security). Real extraction produced 4 docs / 24 chapters / 37 concepts / 149 entities. NexaPay rejected (cache-pollution from post-α smoke test); harvest_hearth held as repeat-run holdout.
    2. ✅ **20+20 quality pairs:** written down in PRD, grounded in the 2026-05-16 real extraction (not hand-picked from raw PDF text). Strongest cross-doc merge signals: company-identity claim across ORG/CMP, named executives (Vargas/Schultz) vs role-only references in CMP, shared external vendor names (Labcorp/Quest/RadNet/SimonMed), 42 CFR Part 2 SUD regime across CLI/CMP. Strongest precision-trap signals: on-site vs external labs, internal vs external audits, same word ("officers", "audit", "insurance", "notification") used for distinct entities.
    3. ✅ **Embedding defaults:** OpenAI `text-embedding-3-large` (3072-dim) / Gemini `gemini-embedding-2-preview` (3072-dim, live-tested green) with `gemini-embedding-001` fallback / Ollama `nomic-embed-text` / Claude none. `text-embedding-004` confirmed dead. ETR disabled in UI until embedding endpoint validates.
  - **Recommended order:** branch SCE off `8225e37` first (lower infra footprint, gets end-to-end first), then branch ETR off the same commit (embedding-selector adds ~half-day before stages 2-3 can run). Both branches share the corpus + quality pairs above.
- [next] **B3 Stage 2 — band-Y clamp during FDL iteration.** Deferred from B3 Stage 1. Only do if visual smoke-test reveals nodes "smearing" out of bands under repulsion. User smoke-tested Stage 1 and confirmed consistent layout across tab switches — so probably won't need this. Small change in `ForceDirectedLayout.runIteration` to clamp `pos.y` to the band's Y range per node level.
- [next] **L2 renderer style for aggregated edges.** Synthesized chapter edges land with `label: "aggregated"` but currently render identically to LLM edges. Audit's "open design question" — render with distinct style (lighter weight, count badge, no linking phrase). `MapCanvasRenderer.swift`. Low priority; only if visual review on the Chapter tab shows the aggregated edges need to be distinguished.
- [next] **N1 — superseded by Connections-panel UX fix + SCE/ETR.** Original idea (hairline transitive connectors between Document nodes sharing chapters) is no longer the lead mitigation. The Connections-panel UX gap was fixed at commit `f1ad018` so the panel matches what the canvas draws. SCE/ETR will produce real cross-doc edges that obsolete the hairline workaround. Keep this item open only as a fallback if SCE/ETR is delayed.
- [next 2026-05-15] Smoke-test the cluster bbox separation visually. Re-extract a multi-section PDF (e.g. `techflow_solutions.pdf`) and confirm: cluster blue rectangles no longer overlap (per the user's image-#1 → image-#2 critique), each concept's entities stay inside its own cluster rectangle, edges still navigable. If clusters look better but edge crossings still feel messy, that's a separate concern — current pass doesn't do edge routing.
- [next 2026-05-15] **Runtime-verify the double-decode fix** (16:00 wrap). Relaunch from Xcode, open a cold tab with a previously-extracted PDF; confirm logs show **one** `[Graph] addNode` block 1→N rather than two consecutive blocks separated by `[GraphStore] Loaded graph`. The `[MultiDocView] loadGraphIfNeeded: hydrated graph` line still reports the same counts. Mechanism: `loadPayload(for:)` returns raw bytes (no intermediate `KnowledgeGraph`), decoded directly into the `@Environment`-injected graph.
- [next 2026-05-15] **Commit today's evening session's uncommitted work** (16:00 wrap) — three commits for revertability: (a) persistence leak fixes + logging build-out (`GraphStore.sweepOrphans`, `PDFViewerApp.runGraphOrphanSweep`, `ProjectsManager.didLoadInitialState` + `allFileURLsForSweep`, `DocumentManager` per-mutation `saveOpenSession`, restoreOpenSession diagnostic logs); (b) "Modifying state during view update" warning fix (`PDFViewerView.swift` onPageChanged dispatch); (c) double-decode fix (`GraphStore.loadPayload` + `MultiDocumentView.loadGraphIfNeeded` refactor). All build-green at HEAD.
- [next] **Smoke-test the build-green-only changes live.** Bundle as one session. *Pause note 2026-05-15 04:22:* user reported "overall things are working" mid-session on the regression sweep but didn't drill into specifics. Today's three additions to the bundle: (l) HighlightSyncBridge stale refs on reopen (`8d9729b`, #39) — close+reopen an extracted PDF, verify highlights reappear; (m) DocumentManager scope release on close (`846f8d4`, #45) — open 2-3 PDFs, quit (saveOpenSession), relaunch (restoreOpenSession), close one tab; verify no Console warnings about unbalanced `stopAccessingSecurityScopedResource`; (n) savePDF scope-after-write (`8d903f1`, #52) — add annotation, wait 1.5s for autosave, close+reopen, verify annotation persists. Resume by relaunching from DerivedData. (a) Backend drift reconciliation (`058936e`) — Claude temperature 0.1 → deterministic extraction; throw on UTF8/JSON decode failure. (b) PDFViewerView split (`a01e05c`) — exercise every annotation mode (highlight/underline/strikethrough/area highlight/ink/rectangle/circle/line/arrow/text/sticky note/select-and-drag), cursor changes per mode (fragile `updateNSView` path now in `PDFViewRepresentable.swift`), outline panel, annotation list panel. (c) GraphStore race fix (`42fe76c`) — encode now runs on caller thread; verify saves still complete during extraction without UI hiccups. (d) Map search debounce (`be82520`) — type into search; matching highlights should appear ~250ms after typing stops. (e) Annotation diff sync (`6f8131c`) — open an extracted document and verify highlights appear; trigger refresh and verify no flicker (no more wipe-then-readd). (f) RecentFilesManager (`d17ada2`) — open files, verify recent list updates without lag. (g) Fit-All / Recenter consolidation (`de7c949`) — verify "Fit All" still centers + scales, Recenter button is gone; no UI gaps. (h) Project files cache + debounce (`40c522e`) — switch projects rapidly, type in file search; sidebar should stay responsive even with extraction running. (i) Drop overlay (`60c6a62`/`26144b6`) — drag a PDF over the drop zone: overlay appears on hover, disappears on leave/drop. Drag the same file twice: only one tab opens. (j) PDF search debounce (`8812622`) — type in search; results appear ~250ms after stopping, deleting clears instantly, Enter forces immediate search. (k) Map onChange coalesce (`6eb0862`) — change zoom level during extraction; map should redraw once, not flicker through two layouts. Each has a one-commit revert if a regression shows up.
- Tier 1 #3 (renderer per-frame allocations) — **deferred until profiled.** Attempted in `fa9f768`, reverted in `22b770e`. See `audits/2026-05-14_simplify-survey.md` Deferred paragraph. If a real perf issue surfaces, the right shape is an `entitiesByParent` index in `KnowledgeGraph` (analogous to `be9814b`'s `labelIndex`), not a renderer rewrite.
- Tier 1 #4 (sequential LLM batches) — **deferred (accuracy over speed).** All three optimization paths (move `proposeEdges` out of per-batch loop, K=3 `TaskGroup` concurrency, larger `batchSize`) trade some concept-graph accuracy for wall-time. See `audits/2026-05-14_simplify-survey.md` Tier 1 #4 Deferred paragraph for the three-option walkthrough. Option (a) — hoist `proposeEdges` to a single end-of-extraction call — is future-scope if wall-time becomes a complaint that outweighs the edge-quality cost.

<!-- Done 2026-05-16 (d600b83..8225e37, pushed): post-α B-series + L2 — B1 (DensityManager strict level filter), B2 (source-aware map→PDF nav), B3 Stage 1 (LevelBandSeeder + cross-tab position preservation), B4 (encodeSubgraph + mergeSubgraph for per-doc scoped save/load), B5 (lossy decode for retired EdgeType), L2 (ChapterEdgeAggregation). Live-verified on NexaPay corpus. Suite 87→120 (+8 FourLevel, +3 LevelBandSeeder, +5 ChapterEdge). Deferred: B6 (Deep partial-response), L1/L3, N1, B3 Stage 2. Detail: audits/2026-05-15_post-alpha-bug-report.md. -->

<!-- Done 2026-05-15 (f980fdc): chat panel persistent indicator gated on aiService.isConfigured + chevron hide-handle at map/chat boundary. Note: title-bar drag-region (~28pt top) silently swallows clicks outside SwiftUI .toolbar — repeat-trap. -->

<!-- Done 2026-05-15 (fa74b44 + 9978130 + d3ddbb6, closes #50): knowledge map layout 3-phase — Phase 1 hierarchy-aware seeding (HierarchyForest + TreeLayoutSeeder); Phase 2 HierarchySynthesis (composite-score top-K theme promotion + BFS subtopicOf assignment); Phase 3 cluster-bbox separation (resolveClusterOverlaps after per-node). Suite 95→120 (+25). -->

<!-- Done 2026-05-15 (8d9729b + 846f8d4 + 8d903f1): PR-B closeout — #39 HighlightSyncBridge sanitizes stale annotation refs on fresh PDFDocument; #45 DocumentManager dedup-before-scope-acquire + needsScopeRelease flag + SecurityScopeAccessing seam; #52 savePDF defer moved inside async completion. All 7 PR-B bugs closed. -->

<!-- Done 2026-05-14 evening (PR-A + PR-B sweep): #47 de7c949 (drop Recenter, fitToContent single-fold; +MapInteractionTests); #38 40c522e (cachedProjectFiles, search debounce); #35/#26 won't-fix-obsolete; #42 60c6a62 (EnhancedDropView collapsed to binding); #43 26144b6 (dedup dropped URLs); #44 8812622 (SearchBarView debounce); #30 6eb0862 (coalesced onChange via LayoutKey). Side-finding: Atlas/UI/MapToolbar.swift dead code (separate ticket). Gotcha: leftover Debug app blocks testmanagerd — kill before xcodebuild test. -->

<!-- Done 2026-05-14 (a01e05c): /simplify Tier 1 #5 — PDFViewerView split into PDFViewRepresentable.swift + PDFOutlinePanel.swift + AnnotationListPanel.swift. PDFViewerView.swift 1549 → 734. -->

<!-- Pattern A resolved 2026-05-11: removed SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor from project.pbxproj (4 occurrences). 2 HighlightSyncBridge methods got explicit @MainActor. Side-effect fix: ProjectsManager save/load race. -->
- Optionally swap `summarizeConcept` for `generateRawResponse` + custom doc-summary prompt if "Summarize the concept '<filename>'" wording produces awkward output once a real run happens. Low priority; only if observed.

<!-- Done 2026-05-14 late (40da3ac..5973d19): PR-C cleanup batch — #31 dropped dead HighlightingPDFView.pageCache, #46 init-injected recentFilesManager (was weak), #36/#46a won't-fix. Grounded chatbot #12 landed (7a03bf7..2b16016, +ChatViewModel/+ChatPanelView, 10 tests). 4 orphan rogue-socket branches deleted post-merge. Unmerged: rogue-socket/issue-11 (guided tour). -->

<!-- Done 2026-05-14 late evening: /simplify Tier 3 fully wrapped — #20 String.sha256HexPrefix16; #26 dropped per-line page.selection waste in TextExtractor; #28 dropped duplicate ScrollTracker observer; #22 map search debounce; #23 HighlightSyncBridge diff-based apply (AnchorKey); #25 RecentFilesManager.addRecentFile dedup-against-memory; #29 backend Decodable DTOs; #21/#18/#19/#16 wrap-up. 11 landed, 4 closed without code. -->

<!-- Done 2026-05-14 evening (6447ba9..940e414): /simplify Tier 2 — #7 typed Notification.Name + drop orphan OpenDocuments observer; #12 URL.deletingPathExtension; #14 guard-let; #15 UserDefaults keys → AppConstants; #6 PDFAnnotation+Kind shim for PDFKit read/write subtype asymmetry. #9 GraphStore.scheduleSave race fixed (42fe76c): encode synchronous, Data captured. Tier 2 #8/#11/#13 held pending profile. -->

<!-- Done 2026-05-14 late afternoon (46ea52c..c83ef3e): /simplify survey (audits/2026-05-14_simplify-survey.md + WORKFLOW.md). Tier 1 #1 (058936e): backend dedup via LLMBackend protocol + LLMResponseParser, Backends/ 593→391; uncovered 5 drifts (Claude temp 1.0 vs 0.1 most consequential). Tier 1 #2 (be9814b): KnowledgeGraph.labelIndex + node(matching:), nodes/edges → private(set), 11 call sites rewritten. -->

<!-- Done 2026-05-09 (0e5d650 + c8cad91 + 7001791): doc migration; Pattern A diagnosis + nonisolated fix on QuadTreeNode/HighlightSyncBridge (42→54 passing); .document summary node (ConceptNode.isDocumentSummary + appendDocumentSummary) + 05-07/08 bug-triage bundle. wip/feature-cherry-pick 32 ahead of main. -->


## Annotations
<!-- Body-drag-translate landed 2026-05-12 — see [active] above. Remaining work tracked there. -->
- Annotation move/resize — drag handles UX, the resize half (TODO #13). **Priority:** medium-high. Now scoped to: corner/edge handle resize (math in `AnnotationGeometry.resized` is unused but tested), selection chrome overlay (NSView on top of `PDFView`, ~80 lines), click-without-drag selection, keyboard delete on selection.
- Verify annotation coordinate conversion accuracy; use PDFKit built-in conversion methods consistently; fix Y-coordinate inversion if needed; dynamic annotation sizing based on content; coordinate-conversion tests; thorough bounds validation (TODO #9).
- Annotation state persistence between sessions; fix `currentHighlight` sync; improve state management patterns; state validation (TODO #10).

## Dark mode
- Dark mode end-to-end appearance validation: test, optimize colors, ensure readability (TODO #13).

## Performance & profiling
- Run Instruments (Time Profiler + Allocations) on large PDFs and address hotspots (TODO #11; Final Review 2026-01-16).
- Add memory leak detection in tests (TODO #5).

## Recent files
- Test recent files persistence across app restarts and system reboots (TODO #2).
<!-- Removed 2026-05-07: stale-bookmark refresh persistence — already implemented in RecentFilesManager.swift:151-184 (likely via commit 7e1c605). -->


## Window state
- Test default fullscreen on different screen sizes (TODO #3).
- Consider user preference for window state (windowed vs fullscreen) (TODO #3).

## updateNSView
- Test state synchronization for updateNSView (TODO #8).

## Tests / CI
<!-- Verified 2026-05-11: tests are wired — scheme `pdf_app1` has a TestAction with `pdf_app1Tests.xctest` as a TestableReference; `xcodebuild test` runs the full suite. Stale CLAUDE.md note removed. -->
- Set up CI/CD with test automation (TODO #16).
- Aim for >80% code coverage (TODO #16).

## Documentation
- Document all extracted constants (TODO #6).
- Code documentation: comments for complex logic, public-API docs, architecture doc, coordinate-conversion logic, annotation system, state management patterns (TODO #7).
- Create developer guide (TODO #7).

## UX research / accessibility
- Research standard PDF viewer UI patterns (Preview.app, Adobe Reader); document best practices (TODO #12).
- Test with accessibility tools (TODO #12).

## Export & sync (future)
- Export annotations: separate file / text / markdown / share (TODO #15). **Priority:** low.
- Cloud sync integration: iCloud, other providers, sync annotations across devices (TODO #15). **Priority:** low.
- Advanced annotation tools: shapes (rectangle, circle, arrow), freehand drawing, stamps, more types (TODO #15). **Priority:** low.

## Project-level search V2
<!-- from: docs/TODO.md #18 -->
- Project-level search bar: searches across PDFs within selected project. **Priority:** medium.
  - UX: scope = selected project (default); group results by file then page; click result opens file + scrolls to page + highlights match.
  - Perf: async + cancellable (typing cancels prior search); progressive/streamed results; hard cap per file and total; avoid re-indexing unchanged PDFs (cache by mtime).
  - Data model: persist per-project search history; persist per-file index metadata (hash/mtime/pages).
  - Implementation: extend `PDFSearchManager` to multi-document; per-file tasks with cooperative cancellation; in-memory index cache for current project session; optional background "index warmup" on project open.
  - Polish: per-file "Searching…" indicator; show skipped (inaccessible/missing) files; "Stop" button.
