# Backlog

Living TODO list. Status tags: `[active]`, `[next]`, `[blocked: <reason>]`, no tag = someday.

## Done

- 2026-06-06 — **Cross-doc bake-off integration landed.** Combined SCE, ETR, and Hybrid work merged through PR #58 into `main` at merge commit `3fd3b8551b975fd46f59cb07820df828af152c8f`. Review-slice PRs #55 Hybrid, #56 ETR, and #57 SCE were closed after landing through #58. Final verification before merge: full integration XCTest `555 tests, 0 failures`; focused SCE/headless/Embedding/Hybrid XCTest `119 tests, 0 failures`; SCE analyzer self-test; Hybrid offline scorer pp1/vitacare `1.000`; Hybrid live lexical sweep `LIMITS="24 60 120"` with row counts matching summaries.

- 2026-06-06 — **SCE-QA-01 implementation complete: stopped whole-page source highlights from fallback anchors.** `ExtractionPipeline.findSourceAnchor` now keeps both block-match and page-text fallback recall, but converts page-sized block/page matches to PDFKit selection bounds when available and `.zero` for page-only fallback anchors; `HighlightSyncBridge.applyPersistentHighlights` skips zero, invalid, off-page, and page-sized anchors so page-only evidence still supports navigation/export without persistent whole-page highlights. Verification: `HighlightSyncBridgeTests` `10 tests, 0 failures`; `ExtractionPipelineStateTests` `15 tests, 0 failures`; full XCTest `402 tests, 0 failures`; old persisted graph artifacts still contain historical full-page anchors but they are skipped by persistent highlight rendering.

- 2026-06-06 — **SCE-QA-02 implementation complete: persistent source highlights refresh across relevant open tabs.** `MultiDocumentView` now refreshes persistent highlights when a tab is selected, when a graph load/no-op/missing/decode-fail path runs for that tab, and when graph node/edge counts change across all open documents. `HighlightSyncBridge.applyPersistentHighlights` now returns per-document diagnostics for nodes, anchors, skipped anchors, added annotations, removed annotations, and live annotations. Verification: `HighlightSyncBridgeTests` `10 tests, 0 failures`; full XCTest `397 tests, 0 failures`; native manual smoke still pending.

- 2026-06-06 — **SCE-QA-03 implementation complete: edge labels attach to the rendered curve.** `MapCanvasRenderer` now computes edge labels from the quadratic Bezier point at `t = 0.5` plus a small curve-side offset instead of placing labels at the off-curve control point; the curve shape and arrow tangent are unchanged, and reciprocal labels retain separation. Verification: `MapInteractionTests` `7 tests, 0 failures`; full XCTest `400 tests, 0 failures`; native manual visual smoke still pending.

- 2026-06-06 — **SCE-QA-04 implementation complete: directional edge semantics are explicit in map labels and the selected-node panel.** `MapCanvasRenderer` now draws a compact tangent-aligned direction marker beside visible edge labels; `KnowledgeMapView.selectedNodeDetail` splits relational connections into Outgoing and Incoming groups with arrow icons and `to`/`from` labels. Verification: `KnowledgeGraphTests` and `MapInteractionTests` `35 tests, 0 failures`; full XCTest `409 tests, 0 failures`; native manual visual smoke still pending.

- 2026-06-06 — **SCE-QA-05 implementation complete: chapter rollup edges use useful labels instead of `aggregated`.** `ChapterEdgeAggregation` now groups child concept edges by chapter-pair/type and labels synthesized chapter edges from the underlying relation text/count, e.g. `rolls up: depends on`, `rolls up: manufacturing dependency`, or `aggregates 2: depends on`; old existing `"aggregated"` labels are refreshed when synthesis runs without duplicating edges. Verification: `ChapterEdgeAggregationTests` `8 tests, 0 failures`; full XCTest `407 tests, 0 failures`; native manual visual smoke still pending.

- 2026-06-06 — **SCE-QA-06 implementation complete: chapter rollups include entity-level semantic edges.** `ChapterEdgeAggregation` now maps concept nodes directly to parent chapters and entity nodes through parent concept(s) to parent chapters, so entity-entity, concept-entity, and concept-concept semantic edges can all synthesize chapter-level rollups; existing rollup labels refresh when child-edge counts change. Verification: `ChapterEdgeAggregationTests` `11 tests, 0 failures`; full XCTest `409 tests, 0 failures`; native manual visual smoke still pending.

- 2026-06-06 — **SCE-QA-07 implementation complete: Document zoom gets document-document relationship rollups.** `ChapterEdgeAggregation` now derives document-level semantic rollup edges directly from concept/entity semantic edges by walking concept/entity -> chapter -> document, skips same-document rollups, and refreshes old generated document labels without treating generated chapter edges as source claims. Verification: `ChapterEdgeAggregationTests` `14 tests, 0 failures`; full XCTest `412 tests, 0 failures`; native manual visual smoke still pending.

- 2026-06-06 — **SCE-QA-08 implementation complete: map layout recompute is instrumented and skips unchanged layouts.** `KnowledgeMapView` now logs layout recompute duration, node/edge counts, zoom, canvas bucket, and FDL iteration count; layout recompute skips when visible node IDs, visible edge signatures, zoom level, expansion generation, and 64pt canvas bucket are unchanged. The graph-shape trigger now watches node IDs and edge signatures instead of node count alone, so relationship-only changes still reach the skip-aware path. Verification: `MapInteractionTests` and `MapInteractionMoreTests` `23 tests, 0 failures`; full XCTest `414 tests, 0 failures`; native Meridian tab-switch visual/perf smoke still pending.

- 2026-06-06 — **Extraction persistence guard complete: failed Fast extraction no longer overwrites saved graphs with partial batch snapshots.** `ExtractionPipeline` now tracks the batch loop outcome, returns before chapter/document finalization on failed or cancelled batches, and defers Fast extraction persistence until the final successful save after chapter/document/rollup enrichment. Verification: `ExtractionPipelineStateTests` `18 tests, 0 failures`; full XCTest `417 tests, 0 failures`; `git diff --check` clean. Intentional tradeoff: in-progress Fast extraction has less crash-recovery persistence, but existing saved graphs are protected from partial failed runs.

- 2026-06-07 — **Batch A cleanup complete: removed proven unmounted UI/state fragments.** Removed `FirstRunView`, `MapSearchView`, `ConceptDetailPopover`, `UnifiedSearchManager`, `UnifiedSearchManagerTests`, `DocumentVerticalTabBar`, `DocumentVerticalTabItem`, `ProjectRowView`, `LoadingStateManager`, and `LoadingOverlay`; also fixed clean-build Swift warnings in `AISettingsView`, `MapInteraction`, and `HighlightSyncBridgeTests`. Verification: `xcodebuild clean test -project pdf_app1.xcodeproj -scheme pdf_app1 -configuration Debug` passed with `414 tests, 0 failures`; no remaining Swift source warnings in the clean test log.

- 2026-06-07 — **Headless SCE verification harness fixed.** Headless runs now mirror startup/status lines to stdout/stderr, accept disposable `--project-storage <projects.json>` and `--graphs-output <dir>` paths, support bounded smoke runs with `--doc-limit <n>`, and exit nonzero when extraction fails instead of reporting false `DONE` lines. Verification: `HeadlessRunnerConfigTests` + `ExtractionPipelineStateTests` passed with `33 tests, 0 failures`; full XCTest passed with `417 tests, 0 failures`; disposable Meridian 2-doc smoke proved project loading/graph isolation and now exits `rc=4` with clear failed file names when the Claude sidecar returns HTTP 502. The full native visual smoke still needs a successful AI extraction backend.

- 2026-06-07 — **Disposable Meridian SCE regeneration succeeded with Codex Agent.** Claude Subscription was confirmed unusable for this run because the Claude CLI returns `403` subscription access disabled; Codex Agent with `gpt-5.3-codex-spark` and low reasoning completed the disposable Meridian 4-doc Fast extraction with `rc=0`, `174` live nodes, `334` live edges, and `4` isolated graph files. Decoded persisted graphs all have `complete` document state, `22` persisted chapter rollups, `4` persisted SCE typed edges, and `1` persisted document-document rollup. A save-order bug found during the smoke was fixed by marking Fast extraction complete before the final `GraphStore.scheduleSave`. Verification: `ExtractionPipelineStateTests` `18 tests, 0 failures`; full XCTest `417 tests, 0 failures`.

- 2026-06-07 — **Native Meridian manual smoke complete for SCE-QA-01 through SCE-QA-08.** Added a hidden UI launch path for disposable smoke data: `--project-storage <projects.json>` and `--graphs-output <dir>` now work without `--headless-extract`, letting the native app load the regenerated Meridian project without copying into the real graph store. Visual smoke opened all four Meridian PDFs from `~/Library/Containers/rogues.pdf-app1/Data/tmp/atlas-meridian-headless-smoke`, confirmed persistent source highlights refresh across tabs and remain bounded to text/table regions, confirmed Document/Chapter/Concept zoom levels render from the regenerated graph, and confirmed persisted rollup labels are non-generic (`rolls up: ...` / `aggregates ...`) with the document-document rollup present (`manufacturing_platform_and_operations -> company_research_and_people`, `rolls up: instance of`). Note: unified `log show`/`log stream` did not emit Atlas entries during this launch, so the visual map render smoke passed but timing-log capture was not independently observed. Verification: Debug build passed; disposable graph artifacts were read in place only, and real project/graph files were not copied or edited.

- 2026-06-07 — **Atlas source-highlight annotation persistence fix complete.** Root cause: historical page-sized source anchors were converted into `atlas:` PDF highlight annotations, and save/save-as wrote those generated source highlights into the PDF alongside user annotations; stale baked-in annotations then reloaded as ordinary PDF annotations. `HighlightSyncBridge` now rejects invalid/page-sized source anchors, removes stale or invalid pre-existing `atlas:` annotations during refresh, adopts valid existing Atlas annotations without duplicating them, and `PDFViewerView` temporarily detaches Atlas-generated annotations before PDF writes so future saves do not contaminate PDF files. Verification: `HighlightSyncBridgeTests` `14 tests, 0 failures`; full XCTest `421 tests, 0 failures`; `git diff --check` clean. The already-contaminated Meridian sponsor sample PDF was deliberately not cleaned or regenerated.

- 2026-06-07 — **Export stack revived in the active map UI.** Added an Export menu to `KnowledgeMapView`'s existing map action controls for Obsidian Markdown, plain Markdown, and JSON exports through `NSSavePanel`, kept `ExportManager` as the formatting layer with shared file-extension/content-type metadata, and removed the obsolete unmounted `MapToolbar` UI. Verification: `ExportManagerTests` `6 tests, 0 failures`; full XCTest `421 tests, 0 failures`; Debug build passed.

- 2026-06-07 — **Dormant merge/correlation stack removed.** Removed the disconnected `GraphMergeEngine`, `MergeProposalView`, `ProjectCorrelationSidebar`, their merge/correlation tests, and the AI-only support surface that existed solely for that workflow (`RawMergeProposal`, `AtlasModel.proposeMerges`, `LLMBackend.proposeMerges`, `LLMResponseParser.parseMergesResponse`, `PromptTemplates.semanticMergeProposal`). This keeps the active cross-document direction centered on SCE/ETR/Hybrid graph extraction rather than an unmounted manual merge flow. Verification: `RawTypesCodableTests` + `PromptTemplatesTests` `23 tests, 0 failures`; full XCTest `407 tests, 0 failures`; Debug build passed.

- 2026-06-07 — **Stale standalone Swift scripts retired.** Removed `pdf_app1/scripts/CollapsibleMapTests.swift` and `pdf_app1/scripts/RunNovakTests.swift`. The former no longer compiled against the current four-level graph model; the latter only tested mirrored standalone copies of older Novak/raw parsing types. Current target-compiled XCTest coverage already covers graph containment/expansion behavior, retired `subtopicOf` lossy decode, and raw extraction Codable/linking-phrase behavior.

## Active / Next

- [next] **Decide whether to clean/regenerate the contaminated Meridian sponsor sample PDF fixture.** The code now rejects stale/page-sized Atlas source highlights and avoids persisting generated `atlas:` annotations on save, but `sample_pdfs/files/meridian_biofab/meridian_biofab_sponsor_programs_customers_and_finance.pdf` still contains historical baked-in `atlas:` annotations unless we intentionally clean or regenerate it.

## Deferred Cleanup
