# Backlog

Durable "someday/maybe" items — distinct from session-level Unresolved (which is "next session"). Each entry: one-line item specific enough to act on cold, optional priority, **Why:** if non-obvious.

<!-- from: docs/STATUS.md (What's Next, as of 2026-04-29) -->
<!-- from: docs/TODO.md (unchecked items, as of 2026-05-04) -->

## Bugs

<!-- Fixed 2026-05-11: RecentFilesManager.autoRemoveStaleFiles never fired (init's sync call read inaccessibleFiles before the async file-check populated it). Restructured init to dispatch autoRemoveStaleFiles as a barrier on fileCheckQueue → main, so it runs as a continuation after async checks. Coverage: testInaccessibleFileMarkedAfterAsyncCheck (tracer), testInaccessibleFileAutoRemovedAfterThreeStaleLaunches (the bug), testRemoveInaccessibleFileResetsStaleCounter (counter-reset on manual remove). -->

<!-- Fixed 2026-05-07 (on wip/feature-cherry-pick, not yet committed):
  - Shared in-memory graph leaks across documents → MultiDocumentView.loadGraphIfNeeded clears when no saved graph
  - deleteProject doesn't clean up → ProjectsManager.deleteProject deletes per-doc + project graphs; call site closes open tabs
  - Two analyze paths use different modes → MultiDocumentView now reads @AppStorage selectedMode and passes to processFullDocument
  - GraphStore URL-only cache key (content invalidation) → StoredGraph wrapper stamps mtime+size; load invalidates on mismatch
  - hierarchyLevel decode default wrong for concept nodes → KnowledgeGraph.swift:109 now defaults based on level
-->
<!-- Fixed 2026-05-08 (on wip/feature-cherry-pick):
  - JSONRepair double-bracket on truncated edges array → JSONRepair.swift tracks closedArray; targeted test passes (commit 3a0f813)
  - .document zoom = picked-node not summary → ConceptNode gains isDocumentSummary; ExtractionPipeline.appendDocumentSummary helper called from both fast and deep pipelines; DensityManager prefers summary node (uncommitted, build green, tests blocked by testmanagerd hang)
-->
<!-- Fixed 2026-05-15: "Modifying state during view update" warning when switching between PDF tabs (fired on tab 2+, not tab 1). Cause: PDFViewRepresentable.updateNSView reassigns nsView.document = pdfDocument; PDFKit synchronously posts PDFViewPageChanged on that thread; coordinator.handlePageChange synchronously calls onPageChanged(pdfView.currentPage); the PDFViewerView closure writes `currentPage = page` — an @State mutation mid view-update. Tab 1 escapes because makeNSView runs before observers are wired into the live update cycle. Fix: wrapped the assignment in DispatchQueue.main.async in PDFViewerView.swift onPageChanged closure (~line 196), deferring the @State write to the next runloop tick. No tests added (warning is observed via os_log + Console; no API surface). -->
<!-- Fixed 2026-05-15: Stale closures in `PDFViewRepresentable.Coordinator`. The three closure props (`onPageChanged`, `onAnnotationsChanged`, `onTextAnnotationRequest`) were declared `let` and set once in `init`, so the coordinator kept tab-1's closures across tab switches — but each closure captures `pdfDocument` (a `let` on the View struct) by value at closure-creation time. The View struct (and its closures) is rebuilt on every render with the current `pdfDocument`, but `updateNSView` only refreshed `annotationMode` / `highlightColor` / `onAnnotationError` / `undoRedoManager` and never reassigned the closures. Observable symptom: open tab 2+, scroll → PDFKit fires PDFViewPageChanged → coordinator runs tab-1's stale closure → `tab1Doc.index(for: tab2Page)` returns NSNotFound → toolbar page indicator stuck at "1". Other two closures had the same latent shape (`onAnnotationsChanged` callbacks routing through stale state, `onTextAnnotationRequest` likewise). Fix: `let` → `var` on the three Coordinator props (lines 157-159) plus three assignments in `updateNSView` (~line 110) — same pattern as the existing refresh for `annotationMode` etc. No tests added (SwiftUI/PDFKit coordinator-state freshness has no clean unit-test harness). Smoke-test: open ≥2 PDFs, scroll the second, verify toolbar page number tracks scroll; optionally annotate / add a sticky note on tab 2 to exercise the other two closures. -->
<!-- Fixed 2026-05-15: Double-decode on graph hydration. `MultiDocumentView.loadGraphIfNeeded` called `GraphStore.shared.load(for:)`, which decoded the on-disk JSON into a fresh `KnowledgeGraph`, then re-encoded that intermediate instance and decoded *again* into the @Environment-injected `knowledgeGraph` (single instance, can't be swap-assigned). Each cold tab hydration ran the decoder twice + the encoder once for ~250 KB / 126 nodes — visible in logs as two consecutive `[Graph] addNode` blocks counting 1→126 separated by the `[GraphStore] Loaded graph` line. Fix: added `GraphStore.loadPayload(for:) -> Data?` that returns the raw payload bytes after the same mtime/size invalidation check; `load(for:)` now delegates to it. `loadGraphIfNeeded` calls `loadPayload` and feeds the bytes straight to `knowledgeGraph.decode(from:)` — one decode pass, no encode. The redundant `[GraphStore] Loaded graph for X: N nodes` log line no longer fires from this path (the `[MultiDocView] hydrated graph` line already reports the same counts). -->

## Active / Next

- [active 2026-05-12] Annotation move/resize — body-drag-to-translate landed on `main` (via PR #49, commits `30a884d` → `690e0a6`) and **user-confirmed working** during PR #49 smoke testing. Remaining for full feature: corner/edge resize via `AnnotationGeometry.resized` + `handle(at:)` (math is in but unused), selection chrome overlay (handles + selection rect), click-without-drag selection, keyboard delete. **Priority:** medium-high.
- [next 2026-05-14] Port `rogue-socket/issue-11` (guided tour, #11) — last unmerged branch. ~700 unique lines: new `Atlas/Tour/` directory (`GuidedTour`, `TourGenerator`, `TourPlayer`, `TourPlaybackView`, `TourPlayerTests`) + `MapInteraction.focusOnNode` + `KnowledgeMapView` wiring. Apply same audit-diff-adapt-commit pattern used for grounded-chatbot today. Will need a real port (not an already-merged finding).
- [next 2026-05-14] PR-A progress: 3 of 6 closed (#47 ✅, #38 ✅, #35 closed obsolete). Remaining are gated/deferred: #29 (FDL — adjacent to deferred Tier 1 #3), #28 (gated on deferred #27), #27 (deferred no-profile). All three need profiling evidence before reopening.
- [active 2026-05-17] **Cross-doc merging — SCE branch LIVE; ETR branch pending.** `feature/sce-cross-doc` created off `8225e37` (post-α HEAD) on 2026-05-16 02:30 — working tree clean, zero commits ahead. ETR branch (`feature/etr-cross-doc`) deferred until SCE proves end-to-end on Gemini per integration decision #4. Design doc: `prds/2026-05-15_4-level-knowledge-graph.md` §"Cross-Doc Merging — Two Approaches" (lines 139-222) + §"Comparison Conditions" (224-268) + §"Locked-in prep items — 2026-05-16". Decision #11 mandates building BOTH on separate branches, A/B testing, winner merges back to `main`. **SCE step 1 plan + file-touch list + tests** captured in `~/.claude/sessions/atlas/2026-05-16.md` under `/wrap before context clear`.
  - **SCE — Sequential Cumulative Extraction** (`feature/sce-cross-doc`). Doc N's every batch carries cumulative state (every node + edge from docs 1..N-1) in the prompt header. LLM prompted: "reuse only when same real-world thing; type advisory." Atomic per-doc commits. Optional final canonicalization LLM pass. **Infra: low** — no new backend, no new settings. **Risks:** quadratic prompt tokens (alarm at >50% context window), anchoring (doc 1 dominates), hallucinated reuse, granularity drift. **Observability from day 1:** `[SCE] doc=X batch=Y prompt_tokens=N` per batch, `new/reused/new_relation_types` per doc, reuse-mapping audit log.
  - **ETR — Extract-Then-Resolve** (`feature/etr-cross-doc`). Four stages: (1) independent per-doc extraction (parallel-eligible), (2) pool + embed `label + ": " + type + summary` + brute-force pairwise candidates, (3) tiered resolution — auto-merge (exact match or sim > 0.95), batched LLM adjudication for sim 0.85-0.95 (~15-20 pairs/call), auto-reject for sim < 0.85, (4) merge + canonical labels + level promotion + edge dedup. **Infra: higher** — new Embedding Model selector in Settings (OpenAI text-embedding-3-small/-large, Gemini text-embedding-004, Ollama nomic-embed-text; Claude has none). ETR disabled in UI when no embedding configured; SCE still works. **Thresholds are placeholders** — must be tuned empirically; re-run only stages 2-4 to retune (cheap).
  - **A/B rubric (frozen before either runs):** held constant = corpus + extraction LLM + machine + α-foundation. Captured = wall-clock, token cost, total nodes, total edges, cross-doc shared entities (`sourceAnchors.count > 1`), cross-doc edges. Quality = 20 should-merge + 20 should-not-merge pairs hand-graded → precision + recall. Re-run: SCE shuffles doc order; ETR varies threshold.
  - **Three prep items the PRD requires before any branch code lands — LOCKED IN 2026-05-16** (full detail in PRD §"Locked-in prep items — 2026-05-16"):
    1. ✅ **Test corpus:** `vitacare` 4-PDF set in `sample_pdfs/files/vitacare_*.pdf` (organization_and_people, clinical_services_and_pricing, patient_experience_and_operations, compliance_quality_and_security). Real extraction produced 4 docs / 24 chapters / 37 concepts / 149 entities. NexaPay rejected (cache-pollution from post-α smoke test); harvest_hearth held as repeat-run holdout.
    2. ✅ **20+20 quality pairs:** written down in PRD, grounded in the 2026-05-16 real extraction (not hand-picked from raw PDF text). Strongest cross-doc merge signals: company-identity claim across ORG/CMP, named executives (Vargas/Schultz) vs role-only references in CMP, shared external vendor names (Labcorp/Quest/RadNet/SimonMed), 42 CFR Part 2 SUD regime across CLI/CMP. Strongest precision-trap signals: on-site vs external labs, internal vs external audits, same word ("officers", "audit", "insurance", "notification") used for distinct entities.
    3. ✅ **Embedding defaults:** OpenAI `text-embedding-3-large` (3072-dim) / Gemini `gemini-embedding-2-preview` (3072-dim, live-tested green) with `gemini-embedding-001` fallback / Ollama `nomic-embed-text` / Claude none. `text-embedding-004` confirmed dead. ETR disabled in UI until embedding endpoint validates.
  - **Recommended order:** branch SCE off `8225e37` first (lower infra footprint, gets end-to-end first), then branch ETR off the same commit (embedding-selector adds ~half-day before stages 2-3 can run). Both branches share the corpus + quality pairs above.
- [next] **B3 Stage 2 — band-Y clamp during FDL iteration.** Deferred from B3 Stage 1. Only do if visual smoke-test reveals nodes "smearing" out of bands under repulsion. User smoke-tested Stage 1 and confirmed consistent layout across tab switches — so probably won't need this. Small change in `ForceDirectedLayout.runIteration` to clamp `pos.y` to the band's Y range per node level.
- [next] **L2 renderer style for aggregated edges.** Synthesized chapter edges land with `label: "aggregated"` but currently render identically to LLM edges. Audit's "open design question" — render with distinct style (lighter weight, count badge, no linking phrase). `MapCanvasRenderer.swift`. Low priority; only if visual review on the Chapter tab shows the aggregated edges need to be distinguished.
- [next] **N1 — superseded by Connections-panel UX fix + SCE/ETR.** Original idea (hairline transitive connectors between Document nodes sharing chapters) is no longer the lead mitigation. The Connections-panel UX gap was fixed at commit `f1ad018` so the panel matches what the canvas draws. SCE/ETR will produce real cross-doc edges that obsolete the hairline workaround. Keep this item open only as a fallback if SCE/ETR is delayed.
- [next 2026-05-15] Smoke-test the cluster bbox separation visually. Re-extract a multi-section PDF (e.g. `techflow_solutions.pdf`) and confirm: cluster blue rectangles no longer overlap (per the user's image-#1 → image-#2 critique), each concept's entities stay inside its own cluster rectangle, edges still navigable. If clusters look better but edge crossings still feel messy, that's a separate concern — current pass doesn't do edge routing.
- [next 2026-05-15] **Runtime-verify the double-decode fix** (16:00 wrap). Relaunch from Xcode, open a cold tab with a previously-extracted PDF; confirm logs show **one** `[Graph] addNode` block 1→N rather than two consecutive blocks separated by `[GraphStore] Loaded graph`. The `[MultiDocView] loadGraphIfNeeded: hydrated graph` line still reports the same counts. Mechanism: `loadPayload(for:)` returns raw bytes (no intermediate `KnowledgeGraph`), decoded directly into the `@Environment`-injected graph.
- [next 2026-05-15] **Commit today's evening session's uncommitted work** (16:00 wrap) — three commits for revertability: (a) persistence leak fixes + logging build-out (`GraphStore.sweepOrphans`, `PDFViewerApp.runGraphOrphanSweep`, `ProjectsManager.didLoadInitialState` + `allFileURLsForSweep`, `DocumentManager` per-mutation `saveOpenSession`, restoreOpenSession diagnostic logs); (b) "Modifying state during view update" warning fix (`PDFViewerView.swift` onPageChanged dispatch); (c) double-decode fix (`GraphStore.loadPayload` + `MultiDocumentView.loadGraphIfNeeded` refactor). All build-green at HEAD.
- [next] **Smoke-test the build-green-only changes live.** Bundle as one session. *Pause note 2026-05-15 04:22:* user reported "overall things are working" mid-session on the regression sweep but didn't drill into specifics. Today's three additions to the bundle: (l) HighlightSyncBridge stale refs on reopen (`8d9729b`, #39) — close+reopen an extracted PDF, verify highlights reappear; (m) DocumentManager scope release on close (`846f8d4`, #45) — open 2-3 PDFs, quit (saveOpenSession), relaunch (restoreOpenSession), close one tab; verify no Console warnings about unbalanced `stopAccessingSecurityScopedResource`; (n) savePDF scope-after-write (`8d903f1`, #52) — add annotation, wait 1.5s for autosave, close+reopen, verify annotation persists. Resume by relaunching from DerivedData. (a) Backend drift reconciliation (`058936e`) — Claude temperature 0.1 → deterministic extraction; throw on UTF8/JSON decode failure. (b) PDFViewerView split (`a01e05c`) — exercise every annotation mode (highlight/underline/strikethrough/area highlight/ink/rectangle/circle/line/arrow/text/sticky note/select-and-drag), cursor changes per mode (fragile `updateNSView` path now in `PDFViewRepresentable.swift`), outline panel, annotation list panel. (c) GraphStore race fix (`42fe76c`) — encode now runs on caller thread; verify saves still complete during extraction without UI hiccups. (d) Map search debounce (`be82520`) — type into search; matching highlights should appear ~250ms after typing stops. (e) Annotation diff sync (`6f8131c`) — open an extracted document and verify highlights appear; trigger refresh and verify no flicker (no more wipe-then-readd). (f) RecentFilesManager (`d17ada2`) — open files, verify recent list updates without lag. (g) Fit-All / Recenter consolidation (`de7c949`) — verify "Fit All" still centers + scales, Recenter button is gone; no UI gaps. (h) Project files cache + debounce (`40c522e`) — switch projects rapidly, type in file search; sidebar should stay responsive even with extraction running. (i) Drop overlay (`60c6a62`/`26144b6`) — drag a PDF over the drop zone: overlay appears on hover, disappears on leave/drop. Drag the same file twice: only one tab opens. (j) PDF search debounce (`8812622`) — type in search; results appear ~250ms after stopping, deleting clears instantly, Enter forces immediate search. (k) Map onChange coalesce (`6eb0862`) — change zoom level during extraction; map should redraw once, not flicker through two layouts. Each has a one-commit revert if a regression shows up.
- Tier 1 #3 (renderer per-frame allocations) — **deferred until profiled.** Attempted in `fa9f768`, reverted in `22b770e`. See `audits/2026-05-14_simplify-survey.md` Deferred paragraph. If a real perf issue surfaces, the right shape is an `entitiesByParent` index in `KnowledgeGraph` (analogous to `be9814b`'s `labelIndex`), not a renderer rewrite.
- Tier 1 #4 (sequential LLM batches) — **deferred (accuracy over speed).** All three optimization paths (move `proposeEdges` out of per-batch loop, K=3 `TaskGroup` concurrency, larger `batchSize`) trade some concept-graph accuracy for wall-time. See `audits/2026-05-14_simplify-survey.md` Tier 1 #4 Deferred paragraph for the three-option walkthrough. Option (a) — hoist `proposeEdges` to a single end-of-extraction call — is future-scope if wall-time becomes a complaint that outweighs the edge-quality cost.

<!-- Done 2026-05-16 (pushed to origin/main at HEAD `8225e37`; live-verified on NexaPay 4-PDF corpus — post-α B-series + L2 from `audits/2026-05-15_post-alpha-bug-report.md`):
  - **Live smoke-test results:** User wiped app state (graphs / projects / recents / OpenSessionBookmarks; preserved Keychain + AI prefs), opened a fresh NexaPay 4-PDF project (`nexapay_*.pdf`), analyzed each PDF under Deep mode. Logs in `pdf_projects/temp_logs.txt` confirm: **B4** per-doc graph files at correctly scoped sizes (57n/89e, 38n/52e, 14n/14e, 55n/78e — NOT cumulative); **L2** `[Pipeline] Synthesized 5/8/2/15 chapter-level aggregated edge(s)` across the project (30 total); **B3 Stage 1** layout stays consistent across Doc/Chapter/Concept/Entity tab switches per user "graphs remain consistent across switches"; **B2** source-link routing + pulse confirmed. **B5** covered by unit test only (no legacy graphs on disk after the wipe).
  - **Connections-panel UX fix during smoke-test** (commit `f1ad018`). User reported "Connections section shows entries but no visual lines" at Document and Entity tabs. Root cause: `MapCanvasRenderer.drawEdges:94` skips containment edges (implicit in level fold), but the node-detail panel was reading `graph.edges(for:)` with no filter — so the count included containment edges the canvas didn't draw. Fix: split into **Connections** (relational only, matches canvas) + **Hierarchy** (containment, labeled by direction with `↘ Contains` / `↖ In`). At Document/Entity tabs the Connections count is honestly "0" (L1/L3 deferred); Hierarchy surfaces fold relationships. Audit N1 (hairline transitive connectors) declined as the heavier alternative.
  - **20 granular commits landed and pushed** (`d600b83` → `8225e37`), build-green at each step. Files modified: KnowledgeGraph.swift, GraphStore.swift, DensityManager.swift, ForceDirectedLayout.swift, KnowledgeMapView.swift, MapInteraction.swift, MultiDocumentView.swift, PDFViewerView.swift, ExtractionPipeline.swift, FourLevelGraphTests.swift, audits/2026-05-15_post-alpha-bug-report.md, backlog.md. New files: LevelBandSeeder.swift, LevelBandSeederTests.swift, ChapterEdgeAggregation.swift, ChapterEdgeAggregationTests.swift.
  - **Per-fix detail (compressed):**
  - **B5 — Lossy decode for legacy graphs.** Custom `KnowledgeGraph.CodableRepresentation.init(from:)` decodes the `edges` array per-element via a private `LossyEdge` wrapper. Edges whose `type` field doesn't match a current `EdgeType` enum case (e.g. retired `subtopicOf`) are skipped instead of throwing. Skip count logs as `[GraphStore] Skipped N edge(s) with unknown types during decode`. The catch-block `clear()` path that used to wipe in-memory state on legacy decode is now unreachable for this class of failure. Nodes didn't need lossy treatment — the new `NodeLevel` cases are a superset of pre-migration values. Test: `FourLevelGraphTests.test_decode_skipsEdgesWithRetiredEdgeType_keepsNodesAndOtherEdges`.
  - **B4 — Per-doc save filtering + multi-tab merge load.** Two-part fix. `KnowledgeGraph.encodeSubgraph(for: URL) -> (data, nodeCount, edgeCount)` filters to nodes anchored in URL + edges between scoped nodes + this URL's processing state. `GraphStore.scheduleSave` uses it; new saves are correctly scoped. `KnowledgeGraph.mergeSubgraph(from:scopedTo:)` decodes (via the B5 lossy decoder), scope-filters defensively (legacy bloated files self-clean on load), and merges into receiver — does NOT `clear()`. `MultiDocumentView.loadGraphIfNeeded` switched from `decode(from:)` to `mergeSubgraph`. Dropped the also-harmful `clear()` on the no-saved-graph branch (was wiping other tabs' nodes). `GraphStore.loadProjectWideGraph` uses `mergeSubgraph` for the same defense. 4 new tests in `FourLevelGraphTests`: encodeSubgraph scopes nodes; encodeSubgraph drops cross-doc edges; mergeSubgraph preserves existing nodes; mergeSubgraph drops foreign-anchored nodes from legacy bloated payload.
  - **B1 — Drop activeNode/pinned carve-out in DensityManager.** `visibleNodes` is now `graph.allNodes.filter { $0.level == target }` — strict level filtering, no carve-out. `activeNodeID` param dropped (was unused after carve-out went away); single caller `KnowledgeMapView.visibleNodes` updated. Replaced `FourLevelGraphTests.test_densityManager_pinnedNode_visibleAtAllLevels` with `test_densityManager_pinnedNode_notVisibleAtNonMatchingLevels` — pinned entity is visible at the entity tab and NOT at document/chapter/concept tabs.
  - **B2 — Source-aware map→PDF navigation.** `KnowledgeMapView.onNavigateToPage` callback signature gained a leading `URL` (source document). Call site at `KnowledgeMapView.swift:371` passes `anchor.documentURL`. `MultiDocumentView`'s closure routes: same tab → post immediately; else `documentManager.openDocument(sourceURL, projectID:)` which handles already-open via `.alreadyOpen` and new tabs via `.success`, then dispatches the notification via `DispatchQueue.main.asyncAfter(deadline: .now() + 0.15)` so the destination `PDFViewerView` has mounted + subscribed. `.tooManyTabs` / `.fileNotReadable` / `.invalidPDF` produce `alertManager.showAlert` toasts. `PDFViewerView`'s `.onReceive(.navigateToPage)` reads `sourceDocumentURL` from userInfo; if present and doesn't match own `pdfDocument.documentURL`, no-op. Backward-compat preserved: Command Palette + Chat panel don't pass URL so the active tab handles them. No unit tests added — UI / NotificationCenter / PDFKit integration; live smoke-test is the verification path. The 150ms dispatch is a heuristic; audit flagged as a race risk. Follow-up shape if flaky: `.onChange(of: documentURL)` inside `PDFViewerView` to pop a deferred page-scroll.
  - **B3 Stage 1 — Band seeding + cross-tab position preservation.** New `Atlas/Renderer/LevelBandSeeder.swift` (pure functions): each `NodeLevel` maps to a band Y (`.document` 0.15h, `.chapter` 0.40h, `.concept` 0.65h, `.entity` 0.85h); `seed(...)` sub-clusters by parent (entities↔concept via `containsEntity`, concepts↔chapter via `containsConcept`), 3-col tile within each cluster. `ForceDirectedLayout.computeLayout` now also builds `parentChapterByConcept` from `containsConcept` edges and seeds newly-seen nodes via `LevelBandSeeder.seed`. Group centers fall on the band Y of the first member. **Cross-tab stability:** new `validNodeIDs: Set<UUID>?` parameter scopes position-eviction to the FULL graph IDs (not visible-set), so off-tab positions survive tab switches. `MapInteraction.fitToContent` got `visibleIDs: Set<UUID>?` so per-tab zoom-fit ignores positions in other bands; three call sites in `KnowledgeMapView` updated. **Stage 2 deferred:** band-Y clamping during FDL iteration is not in this commit — Stage 1 alone may be sufficient. 3 unit tests on the pure seeder.
  - **L2 — Chapter↔Chapter edge synthesis.** New `Atlas/AI/ChapterEdgeAggregation.swift`: `synthesize(in: KnowledgeGraph) -> Int` walks every non-containment concept↔concept edge, projects to chapter-level edges via `parents(of:edgeType:.containsConcept)`, dedupes by tuple, marks edges with `label: "aggregated"` and confidence `0.7`. Multi-parent concepts produce one chapter edge per (parent, parent) pair. Idempotent across re-extraction. Wired into both fast and deep pipelines after `appendDocumentSummary`. 5 tests: cross-chapter projects, same-chapter skip, containment-edge skip, idempotency, multi-parent fan-out. Renderer style left as-is — synthesized edges use the same render path as LLM edges; the `label` marker is the discriminator for future visual differentiation.
  - **Test suite delta:** FourLevelGraphTests 17 → 22 (+5: B5, B4×4). LevelBandSeederTests new (3, B3). ChapterEdgeAggregationTests new (5, L2). Bundle counts: 46/46 across FourLevelGraphTests + MapInteractionTests + ClusterOverlapTests + LevelBandSeederTests + ChapterEdgeAggregationTests + DocumentManagerTests + HighlightSyncBridgeTests.
  - **What's deferred:** B6 (Deep Pass 1 partial-response tolerance) until Deep is touched again. L1/L3 indefinitely (cross-doc edges → SCE/ETR; entity edges → user evidence). N1 (Document-tab empty-state hint). B3 Stage 2 (band-Y clamp). L2 renderer-style differentiation. All tracked in `[next 2026-05-16]` above and in `atlas/audits/2026-05-15_post-alpha-bug-report.md`.
-->

<!-- Done 2026-05-15 (chat panel — persistent indicator + chevron hide-handle, `f980fdc`):
  - User reported the chat indicator pill (added 2026-05-14, `a46d2dd`) wasn't visible on a fresh launch even though chat had been used. Root cause: indicator was gated on `chatViewModel != nil`, but `chatViewModel` is `@State` on `MultiDocumentView` — in-memory only, reset to nil every launch. So the pill required a prior ⌘4 *in the same session* to appear. The "hidden conversation" framing in the original commit didn't survive a relaunch.
  - Indicator gate flipped to `aiService.isConfigured` (an existing `@Observable` Bool on `AIServiceManager`, true when an API key is set or Ollama selected). Pill now visible from app launch onward whenever AI is set up. Unread count display still requires `chatViewModel != nil && vm.messages.count > 0`. `aiService.createBackend()` *not* called in body — too expensive, allocates a new transport instance per re-eval.
  - Hide affordance: user wanted a way to dismiss the chat panel without ⌘4. First attempted an `xmark` button in the chat header; AppKit silently swallowed every click. Diagnosed: `PDFViewerApp.swift:85` uses `.windowStyle(.hiddenTitleBar)` + `window.styleMask.insert(.fullSizeContentView)` + `titlebarAppearsTransparent = true` (L107-108). The window's title-bar region (~28pt at the top) stays a draggable region even though invisible — any control in those top pixels gets clicks routed to window-dragging. PDF viewer + sidebar work because their controls register through SwiftUI's `.toolbar { }` modifier, which gives AppKit hint to coexist; the chat panel's hand-rolled header had no such registration. Padding the header down by 36pt worked but looked awful. Reverted.
  - Final shape: replaced the bare `Divider()` between map and chat with a 16pt-wide ZStack containing the separator line + a chevron button (`chevron.right`, 14×36 capsule, `.regularMaterial` background) centered vertically. Lives at the boundary, far below the drag region, registers clicks normally. Chevron points right (panel collapses to the right when clicked). `ChatPanelView`'s `onClose` parameter from the X-button attempt was reverted out (net zero diff).
  - Net: +18/-3 in `MultiDocumentView.swift`. No new tests (visual UI affordance, behavior verified live by user). Build green.
  - Process note: title-bar drag-region gotcha is going to bite again. Any future interactive control rendered in the top ~28pt of the window — anywhere outside `.toolbar { }` — will silently lose clicks. Bottom-trailing affordances (like the indicator pill) and pane-boundary controls (like this chevron) are safe.
-->

<!-- Done 2026-05-15 (knowledge map layout — three-layer rewrite):
  - Background: user reported "huge huge mess, impossible to navigate" for complex graphs. Foundational read of `ForceDirectedLayout.swift` + `MapCanvasRenderer.swift` + the data side identified three layered structural causes (concept-per-group with no clustering, ignored `hierarchyLevel`/`subtopicOf`, cluster bboxes invisible to FDL repulsion). Diagnosis of user's saved graph (`7ccef6a7…json`, 618 nodes) confirmed `hierarchyLevel: null` across the board and zero `subtopicOf` edges — i.e. the LLM-non-compliance root cause of #50. User direction: "proper product work that ships, newly-generated graphs only is fine."
  - Phase 1 — hierarchy-aware seeding (`fa74b44`): new `Atlas/Renderer/HierarchyForest.swift` (pure canonical-parent forest, cycle-safe via back-edge skip, level-0 marker wins over subtopicOf edge noise) + `Atlas/Renderer/TreeLayoutSeeder.swift` (bands by hierarchyLevel, children X-centered under parent, subtree-width-aware). `ForceDirectedLayout.computeLayout` seeding/grouping replaced — concept groupKey = subtree root so descendants cluster, tree-seeded initial positions, grid fallback when forest is degenerate. 15 unit tests across two new test files. Net no-op on graphs without hierarchy data (scaffolding for Phase 2 to fill in).
  - Phase 2 — hierarchy synthesis (`9978130`, closes #50): new `Atlas/AI/HierarchySynthesis.swift` runs after `appendDocumentSummary` in `ExtractionPipeline.processPages`. Pure-function pass: skip when LLM produced a usable forest, otherwise score every concept by composite features (generality in-degree × 3 + total non-containment degree × 1 + source-page count × 0.5 + confidence × 1), promote top K = min(15, max(3, ceil(sqrt(N)))) to `hierarchyLevel: 0`, BFS-assign each non-theme to its nearest theme via synthetic `.subtopicOf` edges (label "synthesized"). Idempotent (re-running hits the LLM-respected no-op branch). Tiny graphs (<6 concepts) skip entirely. 7 tests cover the no-op/synthesis/scoring/idempotency/disconnected-components/theme-count-formula matrix. Closing comment on #50 explains which of the 6 original investigation directions this addressed (#5 — promotion heuristic, the keystone) and which were deferred (#1/#2/#3/#4/#6 — LLM compliance polish, no longer blocking once synthesis guarantees structure).
  - Phase 3 — cluster bbox separation (`d3ddbb6`): user smoke-tested Phase 1+2 on fresh extraction, reported clusters still visually overlapping with edges crossing through unrelated regions (image-#1 critique vs. their manual rearrangement image #2). Diagnosed: `MapCanvasRenderer.drawGroupBackgrounds` draws each concept-plus-entities cluster as a labeled rectangle, but FDL only repels individual node centers — the cluster *rectangles* are an emergent visual unit FDL doesn't know about. New `ForceDirectedLayout.resolveClusterOverlaps(nodes:)` runs after the existing per-node `resolveOverlaps` — computes cluster bbox with renderer's exact padding (40 horizontal, 30 top, 50 bottom), detects pairwise overlap, pushes overlapping clusters apart as rigid units (concept + entities translate together so FDL's intra-cluster placement is preserved). 30-iteration cap with early-exit. Method made internal for testability. 3 tests in `ClusterOverlapTests.swift` cover overlap-separates / structure-preserved / no-overlap-no-change. Visual smoke-test still pending — committed on user's "fine for now" sign-off.
  - Suite: 95 → 120 (+25 tests across the session). Build green at each commit. Closed #50 via auto-trailer + framing comment.
-->

<!-- Done 2026-05-15 (PR-B closeout — last three audit bugs + one follow-up):
  - #39 HighlightSyncBridge stale annotation refs on fresh PDFDocument (`8d9729b`): foundational re-read revealed Tier-3 #23 (`6f8131c`)'s URL-in-AnchorKey already isolated cross-document state during a single session; what remained was downstream — on close+reopen, `activeAnnotationMap` retained stale annotations whose `.page` pointed at the released document, so the diff put all keys in `kept` and the reopened doc silently got no highlights. Fix sanitizes tracked entries whose `annotation.page?.document !== document` before computing the diff. Stale keys fall into `toAdd`, fresh annotations attach to the new document. Failing-test-first: `testApplyPersistentHighlights_freshDocumentInstance_attachesAnnotationsToNewDocument` proved red on assertion `annotation.page?.document === doc2` before the fix, green after. Audit's "fingerprint check on refresh" framing declined — `6f8131c` already bounded PDFKit ops by delta; the audit's primary cost claim was obsolete.
  - #45 DocumentManager dedup-before-scope-acquire + close-tab scope release (`846f8d4`): audit's "late PDF load failure" framing didn't match the code (no failure path after PDFKit init succeeds), but a real leak hid underneath — duplicate-bookmark `continue` at L236 skipped without stopping the scope acquired at L234. Fix moves the `documents.contains` dedup BEFORE the `start` call so the duplicate iteration never acquires scope. Bonus fixes: `PDFDocumentItem.needsScopeRelease` flag tracks restored-vs-NSOpenPanel-opened docs so `closeDocument` only stops scope when there's a balanced start to release (NSOpenPanel-opened tabs have implicit access; calling stop without start would log an unbalanced-pair warning); stale-bookmark `isStale` flag now logged at info level (was previously silent). `SecurityScopeAccessing` protocol + `DefaultSecurityScopeAccessor` extracted as a testability seam — production wraps URL methods, tests inject a counting fake. `restoreFromBookmarks(_:resolver:)` extracted from `restoreOpenSession` so tests can bypass the XCTest gate (added 2026-05-09 for malloc-double-free reasons) with a pass-through bookmark resolver. 3 tests in `DocumentManagerTests`. Audit's `maxOpenDocuments` knob declined (no user evidence, "no flexibility that wasn't requested").
  - #52 PDFViewerView savePDF defer-before-async (`8d903f1`, filed today as #45's adjacent finding, fixed in same session): `defer { stopAccessing }` at function scope fired on `savePDF` return — immediately after dispatching the write to `DispatchQueue.global` — *before* the background `pdfDocument.write(to:)` ran. Fix moved the stop call inside the existing `DispatchQueue.main.async` completion block so scope stays held across the async boundary. Option A (surgical, 2-line move, no test) chosen over option B (extract `PDFFileSaver` + injected scope accessor + ordering tests, ~100 lines) — the structural fix is small enough that visual review is the appropriate guard and `savePDF` is the only async-write site in the file (`saveAs` is synchronous on NSSavePanel completion). Adjacent concern flagged in the closing comment but not fixed: `PDFDocument` is documented "not thread safe" but the save paths pass the document to background queues while annotation editing mutates it on main — pre-existing race, separate ticket if/when it surfaces user-visibly.
  - PR-B label now empty: all 7 original audit bugs closed (#42, #43, #44, #30, #39, #45 ✅; #26, #35 closed obsolete).
-->

<!-- Done 2026-05-14 (evening PR-A + PR-B sweep):
  PR-A:
  - #47 closed (`de7c949`): dropped the "Recenter" button + handler from KnowledgeMapView/MapInteraction (M1 — Fit All covers the "show me the graph" workflow; the rare zoom-preserving recenter doesn't justify a second button). Single-fold the bbox computation in `fitToContent` (was 4 .map.min/max passes with two intermediate [Double] allocations per coord; now one fold). Perf framing reframed as dedup — audit's "visible jank" claim didn't survive scrutiny (recenter was user-button-only, audit conflated it with fitToContent which has 3 auto-fire callers).
  - New `pdf_app1Tests/MapInteractionTests.swift` (4 tests): empty layout no-op, single position (scale clamped to 2.0), large spread (scale fits smaller dim), mixed bbox distribution. Suite 87 → 91, 0 failures.
  - README L166: dropped stale "recenter" word from `MapToolbar.swift` description.
  - Side-finding (not pulled forward): `Atlas/UI/MapToolbar.swift` has zero call sites — dead code. Worth a separate cleanup ticket.
  - #38 closed (`40c522e`): `MultiDocumentView` now caches resolved project files in `@State cachedProjectFiles: [URL]`. Audit-as-written said "memoize filteredProjects and hasUnprocessedFiles" but the real cost was one level deeper: `filteredProjectFiles` did N security-scoped bookmark resolves per call (a filesystem syscall via `bookmarker.resolveBookmark`), and was called twice per body re-render (once via `hasUnprocessedFiles` toolbar gate, once via `ForEach`). Body re-runs frequently during extraction → 2N syscalls many times/sec. Rebuild now fires only on `selectedProjectID` / `projects.count` / `debouncedFilesQuery` change. Search input debounced 250ms via `.task(id:)` (same pattern as `be82520` map search). Stale-bookmark self-healing preserved (still runs inside `resolveURL` at cache-rebuild time, just not 60×/sec). File-rename inside a project is accepted-lag (count unchanged → no trigger).
  - #35 closed won't-fix: both halves of the audit's framing don't survive scrutiny. Part 1 (toolbar `GeometryReader` + `toolbarIsCompact`) resolved by the 2026-05-12 toolbar relocation to sidebar via `PDFToolbarBridge` — `rg toolbarIsCompact` returns zero hits anywhere. Part 2 (sepia overlay rebuilt every render) conflated value-type construction with render work — `Color` is Hashable/Equatable, SwiftUI diffs structurally, so reconstructing an identical `Color(red: 0.94, ...)` on next body pass produces the same view node and no render work. Same close-shape as #36 earlier today.

  PR-B:
  - #42 closed (`60c6a62`): `EnhancedDropView` collapsed to single `.onDrop(of:isTargeted: $isDropTargeted, perform:)`. Audit framing partially wrong — there was no overlay during any drag because neither stacked `.onDrop` registered a drag-enter callback (`isTargeted: nil`, no `dropEntered` in delegate). Overlay only flashed ~1s post-drop via manual `DispatchQueue.asyncAfter`. New binding-based version: SwiftUI manages overlay visibility (true on drag-enter, false on drag-exit OR drop). Deleted `EnhancedDropDelegate` (70-line over-engineered DropDelegate), `processPendingFiles` duplicate path, `pendingFiles` per-file preview state (was advertised but never functional), and error-feedback overlay (was readable for ~1s, unlikely to be seen). Net −157 / +25. Three call sites of public `enhancedDropZone` extension unchanged.
  - #43 closed (`26144b6`): Dedup dropped URLs by `standardizedFileURL` before passing to consumer. One-line follow-up since #42's new `handleProviders` was doing `urls.append` without dedup. Order-preserving via `Set` + `compactMap`. Audit's references to `validateDrop`/`performDrop` on the old delegate were moot.
  - #44 closed (`8812622`): `SearchBarView` asymmetry (typing no-op until Enter, deleting-to-empty live-clears) fixed via the same debounce pattern landed twice earlier today: `.task(id: searchText)` → `debouncedSearchText` 250ms after last keystroke (instant on clear) → `.onChange` runs `performSearch` (non-empty) or clears (empty). `onSubmit` retained as "skip debounce" for power users.
  - #30 closed (`6eb0862`): Two `onChange` handlers (`graph.nodeCount`, `zoomLevel`) coalesced into one keyed on fileprivate Equatable `LayoutKey` struct. Single recompute path even when both observables change in the same SwiftUI update cycle (e.g. user taps zoom during extraction). `(oldKey, newKey)` preserves prior split-handler behaviors: `fitToContent` only when zoom actually changed, search rerun only when nodeCount changed with active search. Audit's other two concerns (`filteredNodeIDs` recomputed-every-body + "O(n²) contains") were already obsolete from `be82520` (cached `Set<UUID>` lookup is O(1)). `os.Logger` doesn't accept `SemanticZoomLevel` directly → wrapped in `String(describing:)`.
  - #26 closed obsolete: cache-coherence concern moot after `f6b35b6` dropped 2 of 3 cache vars (single var can't have coherence problems with itself). "Cache isn't used on hot path" no longer accurate — renderer reads `cachedFilteredGraph ?? graph` at L82. `graphForCurrentZoom` still allocates but has exactly one caller (`recomputeLayout`) on rare events. Same close-shape as #35.

  - Test-host gotcha: leftover Debug app instance from morning's smoke-test launch (PID 24069, started ~18:50) blocked testmanagerd connection for the full-suite run. Killed at 22:30 with user OK. Note for future sessions: if `xcodebuild test` hangs "before establishing connection," `ps aux | grep pdf_app1` and kill any leftover Debug app first.
  - Net: 5 real fixes + 2 informed-close-obsolete + 4 backlog/audit syncs. Build green at each step; suite 87 → 91 (the 4 new MapInteractionTests).
-->

<!-- Done 2026-05-14 (overnight, second pass):
  - /simplify Tier 1 #5 (PDFViewerView split, commit `a01e05c`): pure file shuffle, no behavior change. Three new files — `pdf_app1/pdf_app1/PDFViewRepresentable.swift` (601 lines: bridge + nested Coordinator), `Atlas/UI/PDFOutlinePanel.swift` (98 lines), `Atlas/UI/AnnotationListPanel.swift` (146 lines). PDFViewerView.swift shrinks 1549 → 734 lines; retains BookmarkManager + HighlightingPDFView at top, PDFViewerView itself, plus TextAnnotationDialog (35) and PDFThumbnailViewRepresentable (19) — too tiny to split. Project's PBXFileSystemSynchronizedRootGroup auto-discovers new files. Build green. Off-by-one extraction caught a missing `}` mid-build; live smoke-test still recommended since no test coverage on PDFViewerView and the known `updateNSView` cursor workaround now lives in the moved bridge file.
-->

<!-- Pattern A resolved 2026-05-11 via structural fix: removed `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` from project.pbxproj (4 occurrences). Only fallout was 2 PDFKit-touching methods in HighlightSyncBridge needing explicit @MainActor. Full suite: 58/6-incomplete → 64/0-incomplete. Side-effect: exposed and fixed a long-standing ProjectsManager save/load race (CombineLatest3 never emitted without projectsSortMode change; load() overwrote in-memory state when file missing). -->
- Optionally swap `summarizeConcept` for `generateRawResponse` + custom doc-summary prompt if "Summarize the concept '<filename>'" wording produces awkward output once a real run happens. Low priority; only if observed.

<!-- Done 2026-05-14 (late):
  - PR-C audit cleanup batch landed on main as 3 commits (range 40da3ac..5973d19, pushed): `4c345f0` dropped dead `HighlightingPDFView.pageCache` machinery (write-only cache + boundsDidChange observer + debounce timer + thumbnail rendering, −77 lines, **#31**); `b9b8f83` dropped `annotationModeLabel`/`annotationUsesColor` from PDFViewerView (superseded by MultiDocumentView equivalents during 2026-05-12 toolbar relocation) plus unused `columnVisibility` `@State`; `5973d19` switched `DocumentManager.recentFilesManager` from `weak var ?` to init-injected `let` (PDFViewerApp constructs both @StateObjects in `init()`; eliminates silent-nil failure mode) and inverted stale `testDeepModeIsUnavailable` test that had been red since `7315aad` flipped Deep available. Net −93 lines. Build green; ExtractionModeTests green. **#31 + #46 (DI half) closed via trailers.**
  - #36 (textAnnotationPoint dead state) closed won't-fix after first-principles reassessment: the `@State` "buffer between callbacks" is the normal SwiftUI pattern; refactor to `.sheet(item:)` required an Identifiable wrapper struct + hand-rolled `Binding(get:set:)` that added cognitive load without solving any defect. Prototype reverted before committing.
  - #46a (zoom format) declined as part of #46 close: comment added explaining the formatter rewrite had unverified `isLenient` interaction with `positiveSuffix` (parsing "150" without suffix may fail under strict mode) and unrequested min/max/allowsFloats restrictions that could reject pre-existing stored values. Original `format: .number` + `Text("%")` pattern is widely used (e.g. Print > Scale) and left alone.
  - Open-issues sweep: spot-checked all 24 open issues. #6 Deep mode close-eligible on 3 of 4 hard criteria but criterion 5 (per-pass progress UI) unverified and 6 (HITL quality) is #50 — kept open. #26 cache coherence partially obsolete (2 of 3 cache vars gone via `f6b35b6`; cache now hot-path-read; but `graphForCurrentZoom` allocates per call + dual `onChange` race remains) — kept open. Everything else verified still applicable.

  Grounded chatbot (#12) landed on main as 8 commits (range 7a03bf7..2b16016): ChatViewModel + ChatPanelView + 10-cycle ChatViewModelTests + MultiDocumentView integration (⌘4 toggle, HStack wrap, lazy toggleChat()). Smoke-tested live; 10/10 tests pass. Port adapted to use main's existing `aiService` env var (not duplicate `aiServiceManager`) and preserved 3-arg `onNavigateToPage` callback. Skipped branch's pbxproj/xcscheme hunks — project uses PBXFileSystemSynchronizedRootGroup which auto-discovers new files.
  - Branch cleanup: deleted 4 orphan branches whose content was already on main under different SHAs — `rogue-socket/fix-resize-lag` (PR #19 + later refinement b6e35dd), `rogue-socket/fix-settings-link` (PR #21, d4aaa5f), `rogue-socket/wire-multi-doc-extract` (PR #20, 997d254; main's version is strictly better, passes `mode: selectedMode`), `rogue-socket/fix-map-drag-zoom` (PR #22, was held by Conductor worktree at ~/conductor/workspaces/atlas/papeete — worktree removed first). Also pruned stale `origin/refactor/24-fdl-barnes-hut` ref.
  - KnowledgeMapView cleanup landed as f6b35b6: removed dead `cachedFilteredNodeCount` + `cachedZoomLevel` @State vars and their writes (4 lines). Vestige of a planned staleness check that was never wired up — actual cache refresh is gated by the onChange handlers that call recomputeLayout. Workspace-wide grep confirmed zero reads pre-removal.
  - Local `rogue-socket/grounded-chatbot` branch deleted post-merge.
  - One unmerged branch remains: `rogue-socket/issue-11` (guided tour subsystem, ~700 unique lines including `Atlas/Tour/*` + KnowledgeMapView wiring + MapInteraction.focusOnNode).
  - Flagged but not pulled forward: HighlightingPDFView pageCache machinery (~70 lines populated-but-never-read) is audit issue #31, slotted for PR-C cleanup batch.
-->

<!-- Done 2026-05-14 (late evening):
  - /simplify Tier 3 cheap-dedup batch — 2 commits direct to main (`8e48cfc..f54af50`, pushed): #20 extracted `String.sha256HexPrefix16` helper in new `Atlas/Utils/String+Hash.swift`, removed the SHA256→hex-16 dup at GraphStore.graphFileURL + AIServiceManager.cacheKey (CryptoKit imports dropped from both); #26 deleted the wasted `page.selection(for: NSRange(fullText))` inside `TextExtractor.extractBlocks`'s per-line loop — the `_ = selection // suppress warning` line was a band-aid pointing at exactly this dead work. Reframe during analysis: the call ran once per line (not once per page as the audit implied), so the impact is a real per-extraction perf win on text-heavy pages, not just dead-code removal.
  - /simplify Tier 3 #28 (`2990b57`): dropped `.PDFViewVisiblePagesChanged` observer from `ScrollTracker` — duplicated `.PDFViewPageChanged` at slower debounce (200ms vs 100ms) in this app's `.singlePageContinuous` mode. Plus equality-guarded the 3 `activeNodeID` write sites in `BidirectionalSyncManager.updateActiveNode` so `@Observable` doesn't notify downstream (`DensityManager`, `MapCanvasRenderer`) for stay-on-same-page scrolls.
  - /simplify Tier 3 #22 (`be82520`): map search filter now @State-cached (was a computed property that ran on every body eval, doubled because referenced in 2 places) AND 250ms-debounced via `.task(id: searchQuery)`. TextField stays bound to the immediate `searchQuery` so input is responsive; `debouncedSearchQuery` drives the filter. Clearing the field is immediate (no 250ms hangover). Re-runs filter on graph.nodeCount changes too so new nodes appearing during extraction with search open get filtered.
  - /simplify Tier 3 #23 (`6f8131c`): `HighlightSyncBridge.applyPersistentHighlights` now diff-based. Storage moves from `[UUID: [PDFAnnotation]]` to `[AnchorKey: PDFAnnotation]` (Hashable struct over documentURL + nodeID + pageIndex + decomposed bounds) so apply can compute desired vs existing per-document and only add/remove the delta. Previously each extraction batch wiped and re-added ALL Atlas annotations — O(N²/2) PDFKit ops total. `refreshHighlights` collapses to a one-line alias. URL in the key also fixes a latent wholesale-overwrite quirk relevant to audit #21. All 5 HighlightSyncBridgeTests pass.
  - /simplify Tier 3 #25 (`d17ada2`): `RecentFilesManager.addRecentFile` dropped from ~2N security-scoped bookmark resolves + N file checks (where N is recent files count, typically 20) to 0 resolves + 1 file check per file open. Dedup now compares URLs against in-memory `recentFiles`; mutations keep `inaccessibleFiles` indices coherent via shift bookkeeping. `removeFiles(at:)` got the same treatment. All 5 RecentFilesManagerTests pass — the async file-check tracer was preserved by scoping the per-URL check inside addRecentFile.
  - /simplify Tier 3 #29 (`01b3873`): three vendor backends (Claude / OpenAI / Gemini) replaced their `JSONSerialization.jsonObject` + chained `as? [String: Any]` casts with private Decodable DTOs (`ClaudeResponse`, `OpenAIResponse`, `GeminiResponse`). Parse collapses to one `JSONDecoder().decode(...)`. Diagnostic improvement: catch block logs `DecodingError` (names the missing/wrong field) alongside the existing raw-response preview, so future API drift is easier to spot.
  - /simplify Tier 3 wrap-up batch (`7221210..f492f40`): #21 made `HighlightSyncBridge.findPassageRects` static (eliminates the duplicate-instance hazard — function was stateless, called via a fresh bridge in PDFViewerView + 5 tests); #18 extracted `String.asConceptType(default:)` / `asEdgeType(default:)` in `Atlas/Utils/String+EnumDecoding.swift` (6 call sites collapsed, kept String field so context-aware fallback stays at the call site); #19 extracted `Debouncer` helper class in `Atlas/Utils/Debouncer.swift` (both real manual debouncers converted, deleted dead `RecentFilesManager.fileCheckWorkItem`, Combine debouncers stayed since they observe Publishers); #16 bundled 9 of MultiDocumentView's 22 @State props into 3 nested state structs (`SidebarState`, `CreateProjectSheetState`, `RenameProjectSheetState`). Also closed #24 partial-by-design, #27 won't-fix (audit's own framing said "marginal"), #30 guidance-only (CLAUDE.md already covers it), #17 won't-fix (audit's "param count = sprawl" framing didn't survive scrutiny — each flagged API has 1-2 callers, Swift named args already self-document).
  - **Tier 3 fully wrapped.** All 15 items resolved: 7 done + 3 done in earlier batches (#20/#22/#23/#25/#26/#28/#29 + #21/#18/#19/#16) = 11 landed; 4 closed without code (#10 won't-fix, #17 won't-fix, #24 partial-by-design, #27 won't-fix, #30 guidance-only).
  - Build green at each step.
-->

<!-- Done 2026-05-14 (evening):
  - /simplify Tier 2 cheap-sweep batch landed direct to main as 5 commits (`6447ba9..940e414`, pushed): #7 typed Notification.Name + dead `OpenDocuments` observer deletion; #12 `URL.deletingPathExtension()` over lowercase-only string strip; #14 guard-let over force-unwrap in ExtractionPipeline; #15 UserDefaults keys (`atlas.ai.backendType/model`, `atlas.ollama.baseURL`) into AppConstants; #6 new `Atlas/Annotations/PDFAnnotation+Kind.swift` shim that re-prefixes PDFKit's read/write slash asymmetry so all kind comparisons unify on `PDFAnnotationSubtype` constants. Build green at each step.
  - Phase-1 "deep why" pass on each item before code. Notable findings: `OpenDocuments` was orphan listener with zero posters (codebase + plists + no AppDelegate URL hook); audit's claim about `PDFSearchManager` raw keys was wrong (already encapsulated as private let); #6's "use an enum" was the wrong shape because PDFKit's read/write API uses two different string forms — a thin getter shim was the right size of fix.
  - /simplify Tier 2 latent-bug track: #9 `GraphStore.scheduleSave` race fixed (`42fe76c`) — encode now runs synchronously on caller's thread, work item captures `Data` (value type) instead of `KnowledgeGraph` reference. Eliminates the concurrent dict read/write hazard between background save and ongoing extraction mutations. #10 (TOCTOU on file load) declined — the `fileExists` precheck the audit wanted removed is deliberate log differentiation between "no saved graph yet" (info) vs "load failed" (error); reasoning captured in survey.
  - Open Tier 2 items: #8, #11, #13 held pending profile data (same reasoning as Tier 1 #3).
-->

<!-- Done 2026-05-14 (late afternoon):
  - Full-codebase /simplify survey across 57 Swift files / ~13.4k lines. Three parallel agents (reuse / quality / efficiency); aggregated to 30 findings in 3 tiers at `audits/2026-05-14_simplify-survey.md`.
  - Tier 1 #1 (AI backend dedup, commit `058936e`): Discovered 5 real drifts via deep-dive analysis (most consequential: Claude's `temperature` knob was unset, defaulting to ~1.0 vs OpenAI/Gemini's 0.1 — silent quality drift). Drafted `audits/2026-05-14_backend-drift-decisions.md` with user sign-off (D2 overrode my recommendation: throw errors, don't surface garbled text). Applied 7 surgical reconciliation edits, then extracted `LLMBackend` protocol with default implementations of all 6 public methods + `LLMResponseParser` enum. Each vendor backend now only implements `transport(prompt:)` + identity. Net Backends/ folder: 593 → 391 lines.
  - Tier 1 #2 (label-lookup dedup + perf, commit `be9814b`): Added `labelIndex: [String: UUID]` to `KnowledgeGraph` with `node(matching:)` for O(1) lookup. Made `nodes` and `edges` `private(set)` after `rg`-verifying zero external writes. Routed `merge(from:)` through silent private `insert(_:)` (no per-node log spam during cross-document merges). Replaced 11 `allNodes.first { $0.label.lowercased() == ... }` call sites with `graph.node(matching:)` across `ExtractionPipeline.swift`, `DeepExtractionPipeline.swift`, `GraphMergeEngine.swift`.
  - Build green at each step. Tests not run. Ollama still works (verified path through `OpenAIBackend` with localhost base URL).
  - Documentation pass (commit `c83ef3e`): added Status table to the simplify survey with per-finding state / commit SHA / partial markers; wrote `audits/WORKFLOW.md` capturing the 5-phase pattern (Analyze → Decide → Reconcile → Refactor → Commit) with anti-patterns, the "intern translation" technique, and Tier 1 #1 + #2 as worked examples pointing at actual commits. Intended as the baseline for future audit work.
  - Five commits landed direct to `main` and pushed: `46ea52c..c83ef3e` (audit docs / backend dedup+reconciliation / KnowledgeGraph label index / backlog sync / docs pass).
  - Open follow-ups: smoke-test live (build-green only); the 3 `allNodes.map { $0.label }` prompt-context sites in `ExtractionPipeline.swift` could become `Array(labelIndex.keys)` opportunistically; Tier 3 #24 (`addNode` `.info` log spam during extraction/decode) still open — only `merge(from:)` is silent now.
-->

<!-- Done 2026-05-09:
  - Doc-migration commit landed as 0e5d650 (13 files, +123/-1048): 7 doc deletions, 2 test-script renames to pdf_app1/scripts/, scaffolding directories. Branch now 30 commits ahead of main.
  - XCTest verification completed for items that did run: test_jsonRepair_closesUnclosedNovakResponse and test_conceptNode_hierarchyLevel_legacyJSON_defaultsByLevel both passed; existing ConceptNode Codable tests pass with the new isDocumentSummary field; 0 regressions.
  - Pattern A diagnosed: SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor in project.pbxproj makes every class implicitly @MainActor; macOS 26.3's Swift Concurrency runtime double-frees during isolated-deinit teardown of arrays of nested class instances. Backtrace from .ips: ___BUG_IN_CLIENT_OF_LIBMALLOC_POINTER_BEING_FREED_WAS_NOT_ALLOCATED → swift::TaskLocal::StopLookupScope::~StopLookupScope() → swift_task_deinitOnExecutorImpl → QuadTreeNode.deinit.
  - macOS 26.3 fixes landed as c8cad91 (4 files, +54/-9): QuadTreeNode and HighlightSyncBridge marked `nonisolated`, XCTest-host guard added to DocumentManager.restoreOpenSession() so tests no longer load user PDFs/graphs. Bundled with yesterday's pending HighlightSyncBridge whitespace-flexible regex fix (multi-line snippet matching) since adjacent in same file. Result: 42→54 passing, BarnesHut and HighlightSyncBridge classes now fully clean; testFindPassageRects_multiLineSnippet_returnsMultipleRects verified.
  - .document summary node + 05-07/08 bug-triage bundle landed as 7001791 (8 files, +191/-22): ConceptNode.isDocumentSummary field with backward-compat decode, ExtractionPipeline.appendDocumentSummary helper called from fast and deep pipelines, DensityManager prefers summary nodes; plus ProjectsManager.deleteProject cleanup, MultiDocumentView.loadGraphIfNeeded clear-on-doc-switch, analyze-mode @AppStorage threading, level-aware hierarchyLevel decode default, GraphStore content-invalidation via StoredGraph wrapper. Two themes were entangled in KnowledgeGraph.swift and DensityManager.swift — bundled rather than split.
  - Branch wip/feature-cherry-pick is now 32 commits ahead of main (was 28 going into today). Working tree clean.
  - isDocumentSummary Codable coverage added to NodeSizingTests.swift (testDecodingWithoutIsDocumentSummaryDefaultsFalse + testIsDocumentSummaryRoundTripsThroughCodable). 9/9 NodeSizingTests pass.
  - Stale backlog item dropped: "Resolve pre-merge STATUS.md conflict in commit 853cbba." Inspection showed 853cbba's actual diff is `DeepExtractionPipelineTests.swift` only — the commit message mentions STATUS.md but no STATUS.md change was ever staged. No conflict to resolve.
-->


## Annotations
<!-- Body-drag-translate landed 2026-05-12 — see [active] above. Remaining work tracked there. -->
- Annotation move/resize — drag handles UX, the resize half (TODO #13). **Priority:** medium-high. Now scoped to: corner/edge handle resize (math in `AnnotationGeometry.resized` is unused but tested), selection chrome overlay (NSView on top of `PDFView`, ~80 lines), click-without-drag selection, keyboard delete on selection.
- Verify annotation coordinate conversion accuracy; use PDFKit built-in conversion methods consistently; fix Y-coordinate inversion if needed; dynamic annotation sizing based on content; coordinate-conversion tests; thorough bounds validation (TODO #9).
- Annotation state persistence between sessions; fix `currentHighlight` sync; improve state management patterns; state validation (TODO #10).

## Dark mode
- Dark mode end-to-end appearance validation: test, optimize colors, ensure readability (TODO #13).

## Performance & profiling
- Run Instruments (Time Profiler + Allocations) on large PDFs and address hotspots (TODO #11; Final Review 2026-01-16).
- Add memory leak detection in tests (TODO #5).

## Recent files
- Test recent files persistence across app restarts and system reboots (TODO #2).
<!-- Removed 2026-05-07: stale-bookmark refresh persistence — already implemented in RecentFilesManager.swift:151-184 (likely via commit 7e1c605). -->


## Window state
- Test default fullscreen on different screen sizes (TODO #3).
- Consider user preference for window state (windowed vs fullscreen) (TODO #3).

## updateNSView
- Test state synchronization for updateNSView (TODO #8).

## Tests / CI
<!-- Verified 2026-05-11: tests are wired — scheme `pdf_app1` has a TestAction with `pdf_app1Tests.xctest` as a TestableReference; `xcodebuild test` runs the full suite. Stale CLAUDE.md note removed. -->
- Set up CI/CD with test automation (TODO #16).
- Aim for >80% code coverage (TODO #16).

## Documentation
- Document all extracted constants (TODO #6).
- Code documentation: comments for complex logic, public-API docs, architecture doc, coordinate-conversion logic, annotation system, state management patterns (TODO #7).
- Create developer guide (TODO #7).

## UX research / accessibility
- Research standard PDF viewer UI patterns (Preview.app, Adobe Reader); document best practices (TODO #12).
- Test with accessibility tools (TODO #12).

## Export & sync (future)
- Export annotations: separate file / text / markdown / share (TODO #15). **Priority:** low.
- Cloud sync integration: iCloud, other providers, sync annotations across devices (TODO #15). **Priority:** low.
- Advanced annotation tools: shapes (rectangle, circle, arrow), freehand drawing, stamps, more types (TODO #15). **Priority:** low.

## Project-level search V2
<!-- from: docs/TODO.md #18 -->
- Project-level search bar: searches across PDFs within selected project. **Priority:** medium.
  - UX: scope = selected project (default); group results by file then page; click result opens file + scrolls to page + highlights match.
  - Perf: async + cancellable (typing cancels prior search); progressive/streamed results; hard cap per file and total; avoid re-indexing unchanged PDFs (cache by mtime).
  - Data model: persist per-project search history; persist per-file index metadata (hash/mtime/pages).
  - Implementation: extend `PDFSearchManager` to multi-document; per-file tasks with cooperative cancellation; in-memory index cache for current project session; optional background "index warmup" on project open.
  - Polish: per-file "Searching…" indicator; show skipped (inaccessible/missing) files; "Stop" button.
